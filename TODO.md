# TODO — SIH26160 working task tracker

> **This file is Claude's own working tracker.** It is updated at the end of **every** turn,
> without being asked. Its job is to answer, at any moment: *what is done, what is in progress,
> what is next, what is blocked, and what is the evidence.*

## Operating rules (for me)

1. **Update every turn.** Before ending any response, update statuses, move finished items to
   Done with an evidence link, and append a line to the Changelog.
2. **Never mark DONE without evidence** — a file path, commit hash, or result file. "I think it
   works" is not evidence.
3. **User requests become tasks automatically.** Anything the user asks for gets an ID here, even
   if they never say "add this to the todo."
4. **Split anything bigger than one sitting** into subtasks with their own acceptance criteria.
5. **Statuses:** `TODO` · `DOING` · `BLOCKED` (say on what) · `DONE` · `DROPPED` (say why).
6. **Priority:** `P0` blocks everything else · `P1` current phase · `P2` next phase · `P3` nice-to-have.
7. The research **freeze** (10-RESEARCH-EXIT-REVIEW.md) still applies. Targeted research is allowed
   when the user asks for it or an experiment raises a concrete question — and gets logged here.

---

## Current focus

**State 2026-09-18: histories merged, build complete, plan §5 P1 fully done.** GitHub `main` (work
from 2026-09-14…16, now T-062…T-067) and this machine (T-051…T-061) are one `main` again (T-068),
with GitHub's black/violet dashboard design (user's choice) and both backends. Verified on the merge:
118/118 unit tests, 69/69 E2E, dataset PASS (82 pcaps), findings differential vs GitHub's old `main`
0 not-allowed (and the GitHub-side hardening changed no finding), dashboard `tsc`/lint/build clean,
real uploads checked in a browser.

**Next: submission (T-046, user's steps).** Re-upload the PPT, record the demo video
(`build/sih/DEMO-SCRIPT.md`), click Submit. The .pptx numbers were updated 2026-09-19 (135 unit tests, 87 pcaps); re-upload it,
and its dashboard screenshots (if any) show the pre-merge look.

**Mentor follow-ups:** B, C1, D done (T-075/T-076/T-077); A dropped; C2 (real AWS tunnel) open, needs the user's AWS account. Plan §5 P2 items remain.

---

## Active and queued tasks

| ID | P | Status | Task | Acceptance criteria | Evidence |
|---|---|---|---|---|---|
| T-046 | **P1** | DOING (user's steps left) | Kumaraguru internal ideathon deck (pptx) + official SIH portal submission (sih.iqubekct.ac.in/submissions/63): all 6 sections filled+saved, 100% readiness. PPT attached (2026-09-12). 2026-09-17: user adopting T-053 numbers (69 unit / 69 E2E / 78 pcaps) and the PQ wording "PQ offered, classical selected; cause not attributable passively" | (a) deck + `build/sih/*` use 69/69/78 and the plan §2.9 stats wording — done 2026-09-17: `PITCH-DECK.md` (bound + EXP-09 live-reproduction wording) and the .pptx (slides 11 and 13: 69 unit tests, 78 pcaps; render checked); the portal still has the old PPT attached → re-upload; (b) demo video recorded; (c) user clicks Submit; (d) 2026-09-18: unit tests now **97** and dataset **82 pcaps** (T-054/055/057); the .pptx (slides 11, 13) still says 69 unit / 78 pcaps. `build/sih/*.md` only quote E2E 69/69, which is unchanged. User's call (no PPT edits by me, per 2026-09-17) | `/Users/bala/Downloads/TunnelScope_SIH26160_Double_Diamond_Deck.pptx` (pptx not re-inspected since 2026-09-12); portal draft 100% |

## Roadmap to submission — complete

Rebuilt 2026-09-16 (T-066): this section was both **stale and corrupted** — it still read
"not yet started / T-030 is next" long after T-030–T-041 shipped, and its table had lost its
rows, leaving an orphaned fragment ("Each beats a stated simple baseline…") from a deleted
row. It was misleading to anyone reading the tracker at a glance, which is the one job this
file has.

Accurate status: every PS deliverable that is ours to build is **DONE** — working prototype,
assessment engine, dashboard, reports, technical documentation, dataset (all in the Done
table below, each with evidence). The deliverables NOT done are the ones that are not code:

| Item | Status | Owner |
|---|---|---|
| Demo video | not recorded | **user** — script ready at `build/sih/DEMO-SCRIPT.md` |
| SIH portal final submit | draft saved, 100% readiness; the attached PPT is the old one — re-upload | **user** — blocked on the video |
| Vendor-appliance validation (Cisco/PAN/Fortinet) | not attempted | **user** — needs licensed images |


## Blocked

_Nothing blocked._ T-063 (Goroda, network-egress-blocked) is superseded, not resolved on its own
terms: the user returned to their earlier Kufica pick instead and supplied that file directly —
see T-064. (2026-09-12: user confirmed **no deadline** → build the full staged system per DEC-023,
not a compressed version.)

---

## Done

| ID | Task | Evidence |
|---|---|---|
| T-080 | Intro → site transition redesigned (user: current one 'not good'; I proposed 5 concepts, user picked C 'into the core'). Diagnosis first: everything faded in at once over a still-bright corridor; two unrelated wordmarks; 0.55s fade vs 1.9s dim-and-drift mismatch. Built: after the hold (now +1.3s) the wordmark fades, the camera accelerates into the core (`RUSH_MS` 1.1s, ease-in, FOV +12°, rings flare as passed); on arrival `IntroTunnel` projects the core's corners to CSS px and calls `onCut`; `Intro` swaps to a DOM square at that exact rect and animates it (Web Animations, 0.65s) to the drop-zone mark's core (`data-intro-target`, 6/32 of the mark), then fades it; page fades in (opacity-only `.page-enter`, 0.3s, so the target doesn't move); background tunnel now goes straight to rest at release (settle machinery removed); skip path unchanged. **Found by measuring**: the square landed 5.5px right because the scroll lock hid the scrollbar and the page re-centred after measuring → `html { scrollbar-gutter: stable }` + lock released at the cut → 0 px error at desktop and 375px. Verified by in-page recording (overlay → square 138.8px → 10.5px on target → cleanup, scroll restored), 3 captured frames (mid-rush, cut, landed), skip path (no square, overlay gone), 375px no overflow, 0 console errors; tsc/lint/build clean | `fleet-dashboard/src/components/intro/{Intro.tsx,IntroTunnel.tsx,timeline.ts}`, `fleet-dashboard/src/components/tunnel/BackgroundTunnel.tsx`, `fleet-dashboard/src/components/dashboard/Intake.tsx`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/src/index.css`, `fleet-dashboard/DESIGN.md` |
| T-081 | `./start.sh` one-command launcher. Checks Python>=3.11 + tshark, creates `.venv` and installs on first run, runs `tunnelscope doctor` (refuses to start if the analysis stack is untrustworthy), installs/builds the dashboard only when `src` is newer than `dist`, starts the engine (127.0.0.1) in the background with a health wait, opens the browser, tails the log; Ctrl-C stops all. Also `-d`, `--dev` (Vite hot reload), `--port`, `--rebuild`, `stop`, `status`, `logs`, `test`, `doctor`. Logs in `logs/` (engine, dashboard, build, setup, start, test; 5 MB rotate), PIDs in `.run/`, both gitignored. Engine now logs each analysis (file, bytes, SAs, seconds) and no longer logs `/health` polls. Verified: fresh venv created and installed, doctor OK, start/status/health/upload (a-start.pcap → 1 SA, 0.20 s)/stop, log lines present, 135/135 tests | `start.sh`, `tunnelscope/api/server.py`, `.gitignore` |
| T-082 | AI layer (user asked for all three). (1) **Random Forest attacker** live on every capture: EXP-05 features (test-enforced identical), trained on first use from shipped `tunnelscope/models/exp05_windows.npz` (380 windows; no pickle), exposure score = confidence × consistency, label never shown (DEC-021), out-of-distribution gate (EXP-05 mux sessions correctly land there), held-out-repetition accuracy 0.99–1.0; new finding `attacker_exposure`. (2) **Anomaly detection** `tunnelscope watch` / `serve --history`: per-tunnel learned normal, posture layer (downgrade/new-failure = high), robust-z traffic layer (≥5 obs), Isolation Forest per tunnel (≥8) and across fleet (≥6 tunnels); stores posture profiles only. Verified: c-m×3 then c-w → 3 downgrades + 2 new failures. (3) **Plain-English explanations** `tunnelscope explain`, glossary for all 9 rules, no language model (an LLM rewrite was built then removed 2026-09-20 by user decision). Dashboard: Explained / Attacker view / Changes tabs, 'changed' chip, AI insights card; verified in the browser, 0 console errors. start.sh: history on by default, `--llm`, `--no-history`. Not verified live: Claude (no credentials here) and Ollama (no model pulled). 18 new tests | `tunnelscope/leakage/attacker.py`, `tunnelscope/anomaly/`, `tunnelscope/explain/`, `tunnelscope/api/server.py`, `tunnelscope/cli.py`, `fleet-dashboard/src/components/dashboard/{TunnelAI,Insights,FleetRegister}.tsx`, `build/07-AI-LAYER.md`, `tests/test_ai_layer.py` |
| T-083 | PS gap closure (plan `build/08-PS-GAP-CLOSURE-PLAN.md`, validation `research/14-PS-VALIDATION.md`). Testbed: 8 traffic shapes incl. e-mail, WhatsApp-like messaging, ICMP (`tgen.py`); EXP-15 = 128 traffic sessions + 9 IKE/DH suite arms + 5 AH arms (all match swanctl). Engine: AH parsing (mode + integrity from plaintext header), `ipsec_protocols`, mode (AH observed / EXP-14 size floor / ACK-size model / else unknown), per-SPI sequence + replay rule, RFC 8221/4303 baseline (confidentiality, AH integrity, 3DES, sequence), sieve 3DES/SHA-384/512 fix, 8-class traffic classifier shown with confidence (DEC-027), threat matrix + risk score + evidence confidence (DEC-028), live analysis `tunnelscope live` / `serve --live-*` (DEC-030), handshake-vs-data cipher labels, reports + HTML dashboard carry all of it. Dashboard: Threats tab, Traffic & exposure tab, Live tab, fleet threat matrix, highest-risk KPI, confidence column. Experiments: EXP-14 (floor 0 false, ~0 coverage; ACK model 44/64, 100%, one post-hoc FP fixed), EXP-15 (5/6 predictions held; mixed-traffic abstain FAILED 29%, stated in output). Verified: 175/175 tests, 85/85 e2e ground truth, dataset 103 PASS, findings diff 0 unallowed, all CLI, air-gap PASS (5 baselines, 33 fields), browser: 8-capture upload, every new tab, Docker live run flagged a mid-run downgrade (risk 95), 375 px no overflow, 0 errors this load. Not done: demo video (user) | `testbed/scripts/{tgen.py,gen_exp15_conf.py,run_exp15_*.sh,run_live_demo.sh,make_replay_pcap.py}`, `tunnelscope/{evidence/protocol.py,risk/,live/,leakage/{attacker,mode_model}.py,rules/rfc8221-4303-ipsec.yaml,report/labels.py}`, `fleet-dashboard/src/components/dashboard/{ThreatMatrix,FleetThreats,LiveView}.tsx`, `experiments/exp14-*`, `experiments/exp15-*`, `tests/test_ps_gaps.py` |
| T-084 | EXP-16 (user asked for all four follow-ups). **Real applications** through the tunnel: new `apps-a`/`apps-b` hosts BEHIND the strongSwan gateways running real Chromium (HTTPS+HTTP2), nginx, OpenSSH (shell + SFTP), Postfix + swaks, Prosody/XMPP, ffmpeg RTP, ping — lab-only servers, no third party, no accounts (scope decided before capturing). 32 real sessions + 32 through **Libreswan 5.4** + 56 extra synthetic repetitions. **P16-1 FAILED**: synthetic-only model scores 0.461 on real apps (file transfer 0.00) — the lab 0.995 was the generator's property, not the model's. P16-2 held (real-trained 0.995), P16-3 held (cross-implementation 1.000), P16-6 held (extra reps delta 0.000), P16-5 held (**mixed-traffic detector**: 92.9% caught, 8.3% false, video+interactive 100%, which fixes EXP-15's failed P15-4). Shipped classifier retrained on everything (LORO 0.986); every prediction now names the 0.46 real-app figure. Found and fixed while capturing: a browser flag fast-forwarded video playback and a 50 MB SFTP finished in 2 s, so both classes had produced no usable sessions. Sanity check on all 64 EXP-16 sessions (in-training, not held out): 62 correct, 0 wrong, 2 abstained (both real browser sessions flagged mixed) | `testbed/images/apps/`, `testbed/scripts/{apps_server_setup.sh,apps_client.py,run_exp16.sh}`, `testbed/configs/exp16/`, `tunnelscope/leakage/mixed.py`, `experiments/exp16-real-apps-cross-impl/` |
| T-085 | Validation against **third-party captures** (Wireshark wiki IPsec samples: 2006–2021, other people's equipment, with their own `esp_sa`/readme ground truth), plus a survey for public datasets and open-source alternatives. New `build/validate_external.py` (downloads on demand, redistributes nothing): checks the IKE suite is read, the cipher sieve never excludes the true family, and mode never contradicts the documented mode. **Found a real bug our 103 lab captures could not**: an ICMP error quotes the header of the packet that caused it, so tshark's `esp`/`ah`/`isakmp` filters also match ESP/AH/ISAKMP headers *inside* ICMP errors; with `occurrence=a` the quoted packet's `ip.len` was read instead of the real one (Wireshark's `ipsec_esp_capture_2`: 312 of 624 matching frames were quoted), which made the sieve exclude the true cipher. First fix (`&& !icmp`) was wrong and the tests caught it — AH does not encrypt, so a real AH packet carrying a ping contains ICMP. Correct fix: each row carries `frame.protocols` and is kept only when the IPsec layer precedes any ICMP layer. Also widened the sieve with DES/Blowfish/Twofish/CAST/NULL/AES-CTR+SHA1 families and made the finding state the table's scope. Verified: external PASS (4 third-party IKE SAs read correctly), 175/175 tests, 85/85 e2e, findings diff 0 unallowed, our own captures unaffected (0 of 103) | `tunnelscope/ingest/tshark.py`, `tunnelscope/evidence/extract.py`, `build/validate_external.py` |
| T-086 | **Handoff to Antigravity** (user near the weekly cap; weaker models will build, Claude reviews). Antigravity reads `AGENTS.md` (v1.20.5+) and workspace rules in `.agents/rules/` (12,000 chars max per file; verified in its docs), so: `AGENTS.md` + 4 short rule files, a 5-task backlog with exact-copy prompts (`handoff/tasks/`), a review protocol (`handoff/REVIEW.md`) and a compact review pack script. The main defence is mechanical, not instructional: `build/check_all.sh` runs every check in one command and `build/guard_diff.py` fails on the classic weak-model mistakes — edits to results files or deleted lines in append-only logs, weakened/skipped tests, files outside the task's scope, secrets, files over 5 MB, allow-list entries without a reason. Verified by committing every mistake on purpose: 6 of 7 were caught first time and the 7th (a key in a brand-new untracked file) exposed a real gap, now fixed; also fixed a design flaw (a new experiment's RESULT.md could not be created). Task 05 (EXP-17, delay/jitter/loss via `tc netem`) was pre-registered and its run script written by Claude BEFORE handing over; a smoke test of the script found a silent failure (Prosody half-starts as root, so XMPP sessions captured almost nothing, hidden by `>/dev/null`), fixed with a listening-port check that refuses to capture, and the same latent pattern in `run_exp16.sh` | `AGENTS.md`, `.agents/rules/`, `handoff/`, `build/check_all.sh`, `build/guard_diff.py`, `experiments/exp17-network-conditions/PREREG.md`, `testbed/scripts/run_exp17.sh` |
| T-092 | **Judge deck, 10 slides, in the website's own design language** (user: 'use the fonts and colours of the website, points and diagrams, easy to explain', then 'the deck must be 10 slides only'). `build/sih/deck/build_deck.js` (pptxgenjs) builds `~/Downloads/TunnelScope_SIH26160_Judge_Deck.pptx`: palette and type from `fleet-dashboard/DESIGN.md` (near-black `#08080B`, hairlines not shadows, one violet accent, green/yellow/red only for status; Geist, Geist Mono, Kufica Bold for the wordmark only), the tunnel mark drawn as concentric rounded squares, speaker notes with timings on every slide. Slides: title, problem+brief, a tunnel hides most of itself (phase diagram), gap+our problem statement, one rule+pipeline, standards+4 models, encryption hides content not shape (native chart from the real training windows), dashboard (real screenshots captured from the live app via headless Chrome), measured-not-claimed (native chart + problem/fix), status/limits/next/team. Fonts: Geist TTFs (OFL) packaged for the presenting machine; Kufica is commercial and deliberately not repackaged. QA: file validates, all 10 slides rendered and checked (substitute fonts in the preview, so fit is approximate). **Found and fixed while doing it**: the Traffic tab printed 'Classes are traffic classes are learned from our lab traffic' (a duplicated phrase from an earlier edit of mine), now fixed with a regression test; a footnote of mine claimed 1,964 windows where the chart uses the 469 clean-tunnel windows; and an earlier claim that our dataset was 'first of its kind' was dropped because another team's public lab dataset exists. Test count now 176 | `build/sih/deck/`, `tunnelscope/leakage/attacker.py`, `tests/test_ai_layer.py`, `README.md` |
| T-093 | **Judge demo kit + presenter's guide** (user: a packet dump to put into TunnelScope, present the prototype, explain every attribute, how it works, and what it is built with). `build/sih/deck/make_guides.py` builds `~/Downloads/TunnelScope_demo_kit/` (6 curated lab captures with a real expected-result table per file, a Docker-free live-demo simulator `play_live.sh`, `DEMO-GUIDE`) and `build/sih/PRESENTER-GUIDE.md` (+ PDF): the pitch, the 7-step pipeline, **all 23 attributes** (what / how we get it / label / why it matters), the 14 rules and 12 threats (read from the YAML and code so they cannot drift), the risk formula and scores, the 4 models, the full tech stack and the likely judge questions. Deck stays at 10 slides; tech stack now printed under each pipeline stage on slide 5. **Found by testing the instructions exactly as written**: the live-demo flow failed because the engine refuses to follow a folder that does not exist and my steps started the engine before creating it; fixed in the guide and script and re-run end to end (learning, learning, normal, then changed with risk 48 to 94). Captures are lab traffic only | `build/sih/deck/make_guides.py`, `build/sih/PRESENTER-GUIDE.md`, `build/sih/deck/build_deck.js` |
| T-079 | Removed the 'Engine on 127.0.0.1' badge from the dashboard top bar (user request, element selected on the page). It was redundant with the red 'analysis engine isn't reachable' banner, which is the only state that matters, so nothing is lost; verified that banner still appears when the built page is served with no engine behind it (plain file server on :8799), and that the badge text is absent with the engine up. The now-unused `engine` prop and `ENGINE_LABEL` removed from `Topbar`/`App`. The footer line 'local engine on 127.0.0.1' is a separate element and stays. tsc/lint/build clean | `fleet-dashboard/src/components/dashboard/Topbar.tsx`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/DESIGN.md` |
| T-078 | (a) **Deck numbers** (user request, overrides the 2026-09-17 'no PPT edits'): `~/Downloads/TunnelScope_SIH26160_Double_Diamond_Deck.pptx` slide 11 '69 unit tests' → 135; slide 13 '69/69 unit tests' → 135/135 and '78 pcaps' → 87; E2E '69/69' left (still true). Edited inside the text runs (formatting kept); backup `…before-2026-09-19.pptx`; both slides rendered via LibreOffice, no overflow. (b) **Liquid-glass cards, no blur** (user: dashboard only, via AskUserQuestion): `.glass` in `index.css` (components layer: 62% dark tint, gradient rim via masked ::before, inset top highlight, 135° sheen, drop shadow; no backdrop-filter) on the upload panel (its `backdrop-blur-md` removed), tab switch, engine pill, KPI bar (gap-px trick replaced by inset hairlines), posture + severity cards, register. Tint raised 55% → 62% after seeing corridor lines cross the faint helper text. Verified in the browser: 0 elements with backdrop-filter, 7 glass surfaces on the sample view, no horizontal overflow. Also fixed the first chart label clipping at ~690px (left margin 64 → 78; label now starts 35px inside the chart). tsc/lint/build clean | `fleet-dashboard/src/index.css`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/src/components/dashboard/{Intake,KpiStrip,Topbar,FleetRegister,SignalTrace}.tsx`, `fleet-dashboard/DESIGN.md`, the .pptx in Downloads (not in repo) |
| T-077 | **Mentor follow-up D — Indian regulatory mapping.** Primary texts only: DPDP Rules 2025 read from the Gazette text (G.S.R. 846(E), 13 Nov 2025; rule 6(1)(a)-(g) verbatim; rule 1(4): rule 6 in force 18 months after publication = 13 May 2027), PIB release for context (₹250 crore penalty), CERT-In Guidelines for Government Entities (30 Jun 2023: 7.1, 4.5.7, 4.5.8/5.8, 3.4). Neither sets algorithms → mapping is evidence, never compliance. `tunnelscope/rules/context/india.yaml` (not a baseline) + `tunnelscope/assess/context.py` + a section in executive and technical reports; package-data updated and checked from an installed wheel. Self-corrections before commit: CERT-In labelled 'guidance, issued', not 'in force'; post-quantum rules removed from DPDP/CERT-In evidence (neither asks for PQ). CERT-In 2022 Directions listed as not covered. 6 guard tests (incl. never says 'compliant'). 135/135 unit | `tunnelscope/rules/context/india.yaml`, `tunnelscope/assess/context.py`, `tunnelscope/report/report.py`, `tests/test_context.py`, `build/06-INDIA-REGULATORY-MAPPING.md`, `pyproject.toml`, `research/13-MENTOR-APPLICATION-REVIEW.md` §5 |
| T-076 | **Mentor follow-up C1 — EXP-13 cloud-VPN-style proposal sets.** Pre-registered first (`7b60c0d`): AWS Site-to-Site VPN default set (read from AWS docs) emulated on strongSwan, 5 arms (legacy DH2/SHA-1 + NAT-T, common MODP-2048, hardened GCM/ECP-384, IKEv1, cloud-initiated). P3/P4/P5/P6 held; **P1 failed on A-START** (unpredicted real bug). Four fixes: (1) `ike_sa_crypto` took the first IKE_SA_INIT response even when it was an INVALID_KE_PAYLOAD error → suite lost; now the last selecting response; (2) DH groups 1/2/5/17/18/22-24 unnamed and scored -1 = FAIL → IANA table, unjudgeable → UNKNOWN (engine `_assert` may return None); (3) RFC8247-DH-MUST compared numbers and passed group 22 (RFC 8247 §2.4 MUST NOT, text fetched) → status table [1,2,5,22,23,24]; DISA kept literal ('16 or greater', SRG text checked); (4) new `ike_offered_dh` + `RFC8247-DH-OFFER`: A-START negotiated MODP-2048 but offered 12 groups incl. 2 and 22 → FAIL. Also AEAD integ note + `ike_prf`. Lab artefact recorded: with 5 configs on one address the responder picked the legacy one (AES-128/SHA-1/MODP-1024), re-run with one config. 129/129 unit (9 new), 69/69 E2E, dataset PASS 87 pcaps, differential vs HEAD 171 changes all allowed (167 new findings + 4 fixed values on the 2 EXP-13 captures), no existing finding changed on the other 134 captures | `experiments/exp13-cloud-vpn-proposals/{PREREG,RESULT}.md`, `testbed/captures/cloud/`, `testbed/configs/cloud/`, `testbed/docker-compose.encap.yml`, `testbed/scripts/run_encap.sh`, `tunnelscope/ingest/tshark.py`, `tunnelscope/evidence/extract.py`, `tunnelscope/assess/engine.py`, `tunnelscope/rules/{rfc8247-ikev2,disa-vpn-srg-v2r6}.yaml`, `tests/test_cloud_vpn.py`, `build/findings-allow.txt`, `dataset/` |
| T-075 | **Mentor follow-up B — on-premise / air-gapped deployment, tested.** Found first, by installing the wheel outside the repo: an installed TunnelScope loaded **zero baselines** (rules resolved relative to the source checkout) and returned `[]`, exit 0, for `pq-downgrade.pcap` (checkout: 8 verdicts incl. FAIL) — absence scored as compliance; the dashboard was missing too. Fixed: `git mv rules tunnelscope/rules` + package-data, `TUNNELSCOPE_RULES_DIR` override, zero baselines → `DependencyError` (exit 3); dashboard shipped as package data (`tunnelscope/web/`, filled by the bundle script, gitignored), server looks there first; explicit `[build-system]`. Built `build/offline/make_bundle.sh` and `build/offline/airgap_test.sh` (Docker: staging container builds the bundle; `--network none` target verifies checksums, `pip --no-index` install, doctor, assess, serve, real IPv6 upload, all under `strace -f -e connect`): **PASS**, 57 MB bundle, tshark 4.0.17 all 29 fields, 8 verdicts / 4 baselines, 0 non-loopback connects (7 → 127.0.0.1:8765, 70 → local nscd socket). Browser: dashboard + sample fleet = 10 requests, all same-origin, fonts bundled. Guards: 2 unit tests, CI step installing outside the repo. `build/05-DEPLOYMENT-ONPREM.md` with limits (Debian 12 arm64 only tested; SHA-256 = integrity not authenticity, sign with org key). Docs updated to the new rules path. 120/120 unit, 69/69 E2E, dataset PASS, findings diff vs HEAD 0 changes | `tunnelscope/assess/engine.py`, `tunnelscope/api/server.py`, `tunnelscope/rules/` (moved), `pyproject.toml`, `build/offline/{make_bundle.sh,airgap_test.sh,AIRGAP-TEST.md}`, `build/05-DEPLOYMENT-ONPREM.md`, `tests/test_assess.py`, `.github/workflows/ci.yml`, `build/00-ARCHITECTURE.md`, `build/03-USAGE-AND-OPERATIONS-PLAN.md`, `build/04-IMPLEMENTATION-PLAN.md` |
| T-074 | Background tunnel brighter (user request). Raising the resting level alone (0.17 → 0.26) put a bright ring through the header subtitle: the nearest rings are the largest on screen. Final: resting 0.30 in the deep corridor, the 5 nearest rings tapered to 22% of that (`NEAR_DIM`). Verification note: the browser pane was drawing ~1 frame per several seconds, so screenshots showed the mid-hand-over (still lit) frame, which is what looked too bright at first; a temporary debug readout proved settle 0.03 after 4s, then a temporary settle-now hook showed the true resting frame (ring opacities 0.30 deep → 0.077/0.024 nearest). Debug hooks removed before commit. tsc/lint/build clean | `fleet-dashboard/src/components/tunnel/BackgroundTunnel.tsx`, `fleet-dashboard/DESIGN.md` |
| T-073 | Tunnel as the page background (user: "go with your recommendations for all four" — top of page only, intro hands over, remove the panel tunnel, keep state reactions). One corridor: `tunnel/layout.ts` shared by intro and background (16 rings, same z, size, glow, camera end-point), rings stretched to the window (≤1.8×) in both. `tunnel/BackgroundTunnel.tsx` (moved from `dashboard/TunnelField.tsx` with `git mv`): fixed behind the page, holds the intro's lit frame while `lit`, then dims to ~17% over 1.9s and shears the projection so the vanishing point rises behind the upload panel; mask fades it by 80% of screen height, vignette, fades on scroll and stops rendering; state reactions kept (drag-over brightens the whole corridor, analysing adds packets). `Intro` gets `onLeave`; `shouldPlay.ts` shared with App. Upload panel frosted, right half now a dashed drop-zone button (opens the file picker) with the status line. **Bug found by the throttled pane**: the settle used the capped per-frame dt, so at 2 fps it would take ~19s; now wall-clock. Verified in the browser: resting look (dim rings behind header, glow through the frosted panel), drag-over (corridor brightens, panel + zone violet), hand-over mid-settle frame, scroll fade opacity 1 → 0.37 at half a screen → 0 past the fold (data on plain black), 375px no overflow, 0 console errors; tsc/lint/build clean | `fleet-dashboard/src/components/tunnel/{BackgroundTunnel.tsx,layout.ts,geometry.ts}`, `fleet-dashboard/src/components/intro/{Intro.tsx,IntroTunnel.tsx,timeline.ts,shouldPlay.ts}`, `fleet-dashboard/src/components/dashboard/Intake.tsx`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/DESIGN.md` |
| T-072 | Intake tunnel made "beautiful and interactive" (user, with screenshots). **Bug found in the user's own screenshot**: tunnel swung hard left because the pointer offset (normalised to the panel but computed from `window` pointer events) was never clamped, so a pointer over the button gave x ≈ −2.5; now clamped to [−1, 1], only followed while the pointer is over the tunnel, eases home otherwise. Visuals: shared `tunnel/geometry.ts` (intro now uses it too) → thin lit tubes + canvas-blur glow, 70 additive "packets" travelling out, ambient light pulses (every 3.4s idle / 1.1s drag-over / 0.55s analysing), breathing core halo, slight hover brighten; interactive click → pulse down to the core → core flash; state semantics, reduced-motion, offscreen pause and SVG fallback kept. Also fixed `Choose captures` `hover:bg-teal/90` (dead token since T-068). Verified in the browser: glow tubes + packets render, pointer over the button leaves the tunnel centred, pointer in the panel corner leans it slightly, click brightens the near ring as the pulse leaves, 0 console errors; tsc/lint/build clean. The pane ran at ~6 fps (throttled), so the pulse's travel was not captured frame by frame | `fleet-dashboard/src/components/dashboard/TunnelField.tsx`, `fleet-dashboard/src/components/tunnel/geometry.ts`, `fleet-dashboard/src/components/intro/IntroTunnel.tsx`, `fleet-dashboard/src/components/dashboard/Intake.tsx`, `fleet-dashboard/DESIGN.md` |
| T-071 | Intro plays on every load (user request), replacing first-visit-only: removed the localStorage flag and `?intro` override (now redundant); reduced motion still never plays it, any key/tap skips, `?intro=hold` kept for stills. Verified in the browser with the old `seen` flag still set: overlay present on 2 consecutive reloads; a key press skips it and restores page scroll. tsc/lint/build clean | `fleet-dashboard/src/components/intro/Intro.tsx`, `fleet-dashboard/DESIGN.md` |
| T-070 | Intro: one faulty light (user: "make one ring fail and catch twice"). Ring 9 of 16 gets its own pattern: stutter → catch (0.28s bloom) → dies with two weak sputters → 0.3s dark → stutter → catches for good (1.22s vs the usual ~0.2s). The corridor waits: rings nearer than it are shifted by `FAIL_STALL_S` = 1.15s (its pattern minus the usual gap, tuned so the next light starts just after the final catch, not 0.13s before it as the first value 1.0 did). Flicker logic moved to a pure `flicker.ts` so the sequence could be checked without WebGL: a text filmstrip run with Node (`--experimental-strip-types`) shows ring 9 flicker 2.10s, catch 2.25s, die 2.55s, dark to 3.05s, re-catch 3.30s, rings 10–15 start 3.40s, all lit by 3.80s. Browser pane was hidden (2 fps), so mid-sequence screenshots weren't trustworthy; the held final frame (`?intro=hold`) confirmed all rings lit. tsc/lint/build clean | `fleet-dashboard/src/components/intro/{timeline.ts,flicker.ts,IntroTunnel.tsx}`, `fleet-dashboard/DESIGN.md` |
| T-069 | First-visit intro: the tunnel lights up ring by ring from the far end toward the viewer, "ghost-movie corridor" style (user's request, choices via AskUserQuestion: three.js over Lottie, full-screen intro, flicker-then-hold, first visit only). The user asked for a LottieFiles MCP: none is installed and none is in the connector registry, told the user. Built `fleet-dashboard/src/components/intro/{Intro.tsx,IntroTunnel.tsx,timeline.ts}`: 16 static rings as thick tube meshes (WebGL lines are 1px), seeded 2–3-stutter flicker per ring then an over-bright bloom settling to steady, accelerating rhythm, camera creep, far core lights first, wordmark over a dark radial pool, fade into the dashboard. First attempt's glow (a flat translucent band) read as a solid purple frame; replaced with a canvas-blur texture. Wordmark hold lengthened after the first browser pass showed it readable for ~0.3s only. Chunk is 2.8 KB (shares the three.js chunk). Verified in the browser: sequence frames dark → core → far ring → all lit → wordmark → dashboard; `?intro=hold` final frame; space key skips, restores scroll, sets the flag; revisit without param doesn't play; cleared flag + plain load plays; hidden tab gives up cleanly; 0 console errors; tsc/lint/build clean | `fleet-dashboard/src/components/intro/`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/DESIGN.md` |
| T-068 | Merged the two diverged histories (user: keep the black/violet design). GitHub `main` had 8 commits from 2026-09-14…16 this machine never fetched (black/violet + Kufica dashboard redesign, chart animation, robustness pass, capture cache), numbered T-051…T-056 — colliding with this machine's T-051…T-056. Local work first pushed to branch `local-sep17-18` so nothing could be lost. Backend: GitHub's `tshark.py` (errors, timeouts, preflight, memo cache) as the base with this machine's changes carried over (RFC 9370 NONE, IKEv1 exchange names, per-SA `ike_sa_crypto` now memoised as `_ike_sa_init_responses` + a per-SA filter that returns a copy, T-057 offsets; 6 new fields added to the preflight list); `cli.py` keeps both `serve` and `doctor`; CI keeps both the differential step and the dashboard job. Dashboard: this machine's structure (intake, three.js tunnel, queue, data-source switch, instrument bar, register panes) re-skinned to the black/violet system (teal→violet, steel→warn, `rounded-2xl`, Kufica wordmark replaces the SVG logo, inferred chip = silver meta tone not the warning yellow), GitHub's animated `SignalTrace` kept with filename-label shortening; fixed a clipped rotated axis label at 375px found in the browser check. `DESIGN.md` v2. GitHub's tasks renumbered T-062…T-067 in this file (commit messages keep their IDs). Verified: 118/118 unit, 69/69 E2E, dataset PASS, differential vs `origin/main` 0 not-allowed, `tsc`/lint/build clean, real uploads of 2 captures through `tunnelscope serve` (IPv6 addresses and CBC evidence rendered, 0 console errors, no overflow at 375px) | `tunnelscope/ingest/tshark.py`, `tunnelscope/cli.py`, `.github/workflows/ci.yml`, `fleet-dashboard/src/**`, `fleet-dashboard/DESIGN.md`, `.claude/launch.json` (`tunnelscope-serve`), `CHANGELOG.md` (merge note), branch `local-sep17-18` |
| T-057 | ESP length offsets for UDP-encapsulated ESP and IPv6 (plan §5 P1-4, ING-6). Built a dual-stack encapsulation lab (`testbed/docker-compose.encap.yml`, `scripts/run_encap.sh`) and captured 4 real tunnels with T2 ground truth. The static-read concern was real and worse than a missing feature: on the UDP-encap **CBC** tunnel the old `ip.len − 28` counted the UDP header and the sieve **eliminated CBC, the true suite**; on IPv6 records had no addresses/lengths and a successful tunnel read as `auth-or-child-failure` (0.9). Fixed in `ingest/tshark.py`: UDP → `udp.length − 16`; IPv4 → IHL not 20; IPv6 → `plen − 8` only when next-header is ESP, else length marked unknown and skipped; IKE sizes over IPv6 counted IPv4-equivalent so the EXP-03/06 size rules keep their meaning. 10 tests (9 fail on the old code). Captures registered in the dataset as `T-057-encapsulation` (excluded split) → 82 pcaps | `tunnelscope/ingest/tshark.py`, `tests/test_encap.py`, `testbed/captures/encap/` (4 pcaps + groundtruth), `testbed/docker-compose.encap.yml`, `testbed/configs/encap/`, `testbed/scripts/run_encap.sh`, `testbed/TOPOLOGY.md`, `dataset/{build_manifest.py,MANIFEST.csv,DATASHEET.md}` |
| T-055 | CVE-2026-78135 message-ID guard (plan §5 P1-2, CVE-2b). With no IKE_AUTH in the capture, fire only if the original initiator's CREATE_CHILD_SA msgid is exactly last pre-auth (IKE_SA_INIT/IKE_INTERMEDIATE) msgid + 1; a gap or a responder-originated child → UNKNOWN. Acceptance met: fires on synthetic + live positives; UNKNOWN on `exp12/rekey-cadence.pcap` with its IKE_AUTH frames deleted (fired before); **0 finding changes on every other capture** (T-054 differential). 4 tests (3 fail on the old code) | `tunnelscope/evidence/extract.py` (`_msgid_gap_before_child`), `tests/test_cve.py` |
| T-054 | Full-findings differential + CI (plan §5 P1-1). `build/findings_diff.py` checks out the base ref in a git worktree, runs both versions' `build_records` over the SAME captures (all 134 locally incl. EXP-05; CI has the tracked ones), compares status/value/confidence per record, exits 1 on any change not matched by `build/findings-allow.txt` (glob + mandatory reason), reports stale entries. CI: `fetch-depth: 0`, base = PR merge-base or the push's `before`. Verified: vs HEAD → 7 changes, all T-057; vs `origin/main` → 41 changes, exactly the 5 captures of the T-053 merge-gate table + T-057, all allowed. 3 unit tests | `build/findings_diff.py`, `build/findings-allow.txt`, `.github/workflows/ci.yml`, `tests/test_findings_diff.py` |
| T-061 | P2 | DONE | Mentor feedback review: explain DLP/cloud terms, validate each mentor point, recommend | Every point given a verdict with evidence; terms explained; proposed follow-ups listed, not started | `research/13-MENTOR-APPLICATION-REVIEW.md`, index row in `research/README.md` |
| T-060 | Integrated `tunnelscope serve` into the main (React) dashboard, using the requested design skills. Skills: **taste** (`design-taste-frontend`, installed) and **impeccable** + **awesome-design-md** (fetched as markdown with user approval; impeccable's launcher binary was deliberately NOT run, its own no-launcher path used). **img2threejs** doesn't exist on this machine and no trustworthy source was found, so it was replaced by the installed `threejs-*` skills (told the user up front). Backend: structured `POST /api/analyze` (same data as the HTML fragment), `serve` hosts `fleet-dashboard/dist` with a resolved-path traversal guard and SPA fallback, built-in page kept at `/basic`, client filenames reduced to a basename, basic page's filename/error now HTML-escaped. Frontend: whole page is a drop target; intake panel with a lazy-loaded three.js tunnel (the product mark in depth) whose motion reports state (idle/drag-over/analysing/result colour/refused), paused off-screen, static under reduced motion, SVG fallback without WebGL; queue with waiting/analysing/done/error+retry states; client-side magic-byte check; data-source switch 'Your captures' / 'Sample fleet' (sample labelled as illustrative); register detail panes Verdicts / Evidence / Not visible from here. Honesty fixes to the old dashboard: removed the fake 'live sources' + 'last scan = page-load time' (now a real `/health` engine status and the real last-analysis time), hero sentence computed from the data instead of hard-coded, 'PQ not selected' wording, KPI cards → one instrument bar, page-load animation removed. `DESIGN.md` written in the awesome-design-md/Stitch format. Verified in a real browser against the real engine with 5 real captures + 1 non-capture (5x 200, bad file refused before upload, 0 console errors), desktop 1440 and mobile 375 (no horizontal overflow), 2 bounded inspection rounds. 80/80 unit (4 new server tests incl. traversal), 69/69 E2E, dataset PASS, dashboard tsc clean | `tunnelscope/api/server.py`, `tests/test_server.py`, `fleet-dashboard/src/{App.tsx,lib/api.ts,lib/fleet.ts}`, `fleet-dashboard/src/components/dashboard/{Intake,TunnelField,Topbar,KpiStrip,FleetRegister,SignalTrace}.tsx`, `fleet-dashboard/DESIGN.md`, `fleet-dashboard/README.md`, `build/03-USAGE-AND-OPERATIONS-PLAN.md` |
| T-059 | Local upload dashboard, replacing 'run a CLI command per file' with 'drop files in a browser tab'. User picked this scope explicitly over 2 lighter options (a live-refreshed fleet-dashboard, or just polishing the one-shot HTML). New `tunnelscope serve [--port]`: stdlib `http.server` only (no new dependency — the T-049 fastapi/uvicorn removal stands), binds `127.0.0.1` only (not a flag), every upload checked against pcap/pcapng magic bytes before it reaches `tshark`, written to a private temp path deleted right after the response (nothing persists), one file's parse failure is its own error card and never crashes the server (mirrors `fleet.py`). Refactored `report/dashboard.py`'s per-SA rendering into `render_sas_html()` so the CLI dashboard and the server render findings identically — one place, not two. 7 new tests (real server on an OS-assigned port, real HTTP, incl. a corrupt-but-magic-matching file and a no-leftover-temp-file check). End-to-end verified in a real browser: started the real `tunnelscope serve` process, uploaded a real pcap's actual bytes (not a mock), watched it render server-side-generated HTML (posture badge, verdicts, evidence table matching the CLI dashboard's own output) with zero console errors and a real 200 in the network log; then a non-pcap upload rendered as a clean error card without disturbing the first result. 76/76 unit tests (was 69), 69/69 E2E, dataset PASS | `tunnelscope/api/server.py`, `tests/test_server.py`, `tunnelscope/report/dashboard.py` (`render_sas_html`), `tunnelscope/cli.py` (`serve` subcommand), `pyproject.toml` (comment updated), `build/03-USAGE-AND-OPERATIONS-PLAN.md` §2 |
| T-056 + T-058 | Wording pass (user: leave the PPT, fix the repo). Human-facing text only; machine contracts kept (CBOM label `DOWNGRADED (...)` is string-matched by `report/fleet.py`, `report/dashboard.py`, `fleet-dashboard/src/lib/fleet.ts` and 3 tests; rule IDs unchanged). CVE: rule title/message and extractor note now say *attempt*, acceptance not visible passively. PQ: 'silently downgraded' → 'offered PQ but negotiated classical', cause not attributable passively (pitch, Q&A, demo step 2, dashboard hero text, DST rule message). Zero-FP claims → 0 FP on 67 judgeable + 2 UNKNOWN, 95% upper bound ≈4.4% (tunnelscope/README, 00-ARCHITECTURE, OPEN-QUESTIONS OQ-31, EXP-09 verdict). Demo 5b retitled 'live exploit attempt from a lab'. 69/69 unit, 69/69 E2E, dashboard `tsc` clean; CVE demo still FAIL (high) | `build/sih/{PITCH-DECK,JURY-QA,DEMO-SCRIPT}.md`, `rules/cve-2026-78135.yaml`, `rules/dst-nqm-pq.yaml`, `tunnelscope/evidence/extract.py`, `tunnelscope/README.md`, `build/00-ARCHITECTURE.md`, `research/registers/OPEN-QUESTIONS.md`, `experiments/exp09-early-childsa-cve/RESULT.md`, `fleet-dashboard/src/App.tsx` |
| T-053 | Fixed `antigravity` to merge quality (user green light + decision: ESP-only SPI changes between one host pair = one tunnel). (1) ESP-only records: one per host pair across rekeys, directional SPIs in capture order (T-052 stress case back to 1 record, timing leakage 1.969 bits as on main); dead multi-SA branch removed. (2) PQ: selection read from the responder's plaintext proposal → `offered-but-not-used` OBSERVED; two-proposal offers no longer 'possible downgrade'; error-only response → UNKNOWN; 0.95/0.9 constants removed. (3) Removed T-051's broken proposal unflattening + its self-asserting test. (4) PFS: 400 B rule MODP-only, other groups UNKNOWN, caveat when group not visible. (5) IKEv1 note cites RFC 9395. (6) `build/04-IMPLEMENTATION-PLAN.md` rewritten from sources fetched from rfc-editor.org (quotes checked verbatim against the raw text): all 42 dossier items adjudicated, plan split into done vs P1–P3. 17 new tests (11 fail on T-051's code). Full differential vs main over 130 pcaps: record counts unchanged; status/value/confidence changes only in the 5 intended captures. DEC-025/026 registered; CHANGELOG entry. 69/69 unit, 69/69 E2E, dataset PASS | `build/04-IMPLEMENTATION-PLAN.md` §3–§6, `tunnelscope/evidence/extract.py`, `tunnelscope/ingest/tshark.py`, `tests/test_extract.py`, `research/registers/DECISIONS.md` (DEC-025, DEC-026), `CHANGELOG.md` |
| T-052 | Adversarial review of the `antigravity` branch (commit `75e4a5e`, T-051 by another team). Re-ran suites (61/61 unit, 69/69 E2E, dataset PASS — confirmed). Ran both branches' extractors over all 130 captures and diffed every finding. **Real improvements:** per-SA `ike_sa_crypto` (main reported UNKNOWN crypto for every SA whenever the pcap's first response was NO_PROPOSAL_CHOSEN); time-ordered ESP attribution (main attached post-failure ESP to failed SAs and called them 'success'). **Regressions/defects:** ESP-only rekeying tunnel split into 4 one-way records (timing leakage 1.97 → 0.0–0.58 bits); proposal transform IDs always `[]` on real pcaps; PQ still inferred from IKE_INTERMEDIATE absence although SAr1 shows the selection in plaintext, and a legitimate two-proposal offer raises 'possible downgrade' (0.9); PFS cutoffs 255/280 B have no real capture behind them (all PFS captures are MODP-2048; measured PFS-off 236–240 B); a failed SA injected mid-tunnel still gets all ESP and 'success' on both branches; the ESP-only 'split fix' fixed a bug `main` never had (main already produced 1 record); one test asserts a note it wrote itself. Plan doc reaches the right RFC 5903 conclusion (64 B, confirmed against RFC text) but the quote is not RFC wording, and roughly half of the dossier's clusters are never adjudicated. Verdict: do not merge as-is → T-053 | Reproduction: `git worktree add` of `main`, dump `build_records` findings for `testbed/captures/**/*.pcap` on both, diff; stress pcaps built with `editcap`/`mergecap` from `cs-aes256gcm16-success-baseline.pcap`, `cs-aes256gcm16-a7.pcap`, `fail-ts-mismatch.pcap`; RFC 5903 §7 and RFC 8247 text fetched from rfc-editor.org |
| T-051 | Adjudicate the 5-model review-panel dossier (ChatGPT-1, ChatGPT-2, Claude, Kimi, Gemini across 9 rounds): verified all claims against RFC text, testbed captures, and codebase; wrote master implementation plan `build/04-IMPLEMENTATION-PLAN.md`; resolved real bottlenecks in `extract.py` and `tshark.py` (PQ ADDKE NONE Transform ID 0 RFC 9370 §2.1 fallback with calibrated confidence and multi-offer responder selection isolation, per-SA crypto extraction in `ike_sa_crypto` with normalized `ispi`, multi-SA timestamp sorting and active SPI correlation in `group_sas`, bidirectional ESP-only tunnel preservation without flow fragmentation, proposal unflattening and initiator-responder proposal matching, legacy IKEv1 detection RFC 8247 deprecation, RFC 5903 §7 64-octet Group 19 and RFC 8031 32-octet Curve25519 calibrated PFS thresholding); added 9 unit tests; 61/61 unit tests pass, 69/69 E2E validation pass, 78/78 dataset pcaps hash-verified. **Superseded in part by T-052/T-053:** the 61-test count, 3-tier PFS thresholds (255/280 B), proposal unflattening and per-SPI ESP-only splitting did not survive review; see T-053 for what was kept. | `build/04-IMPLEMENTATION-PLAN.md`, `tunnelscope/evidence/extract.py`, `tunnelscope/ingest/tshark.py`, `tests/test_extract.py` |
| T-067 | Ingest memo — the Tier-3 item held back from T-066 as the only piece with real refactor risk (user: "implement everything"). Capture reads are memoised on `(path, mtime_ns, size)`; a capture that changes on disk is re-read, never served stale, and the cache is bounded (`TUNNELSCOPE_CACHE_CAPTURES`, default 8 captures, `0` disables). Safety checked before landing rather than assumed: every caller of the three readers only *reads* the returned dicts (grepped for mutation — the sole `.update()` in the package is on an unrelated CBOM dict), so sharing cached objects cannot poison them; the contract is documented and `clear_cache()` exists for long-lived callers. **The original premise was wrong and measurement said so**: I expected this to speed up fleet scans, and it does not — a fleet scan analyses each capture exactly once, so there is nothing to reuse across files (19.7s vs 19.3s over the 35-capture exp06r2 directory = noise). Reported as a non-result rather than dressed up. The real win is *repeated reads of one capture*, which is where the redundancy actually was: `ike_sa_crypto()` is called once per SA and re-parses the whole file each time, so a capture carrying 10 tunnels cost 10 tshark spawns / 1.86s and now costs 1 / 0.19s — O(SAs) collapsed to O(1). The single-capture CLI path (`capture_summary` + `build_records`) drops 5 spawns to 3. Our corpus is 1-SA-per-file, which is exactly why this redundancy went unnoticed; a real gateway capture is the case that benefits. 73/73 unit tests (was 67), 69/69 e2e, dataset PASS | `tunnelscope/ingest/tshark.py`, `tests/test_ingest_cache.py` (new, 6 tests incl. staleness-on-modify and bounded-growth), `tunnelscope/README.md` |
| T-066 | Robustness pass on the failure paths (user: "make the thing more robust"). Every defect below was **reproduced first**, then fixed, then locked with a regression test — the suite previously only exercised captures the tool *can* read. **(1) `fleet` on a bad path reported a clean fleet**: `rglob` on a missing directory yields nothing, so a typo'd path returned `n_scanned: 0`, zero errors, **exit 0** — indistinguishable from a healthy scan in a cron job. Missing dir / not-a-dir / no captures are now `InputError` (exit 2). **(2) No tshark version or field check**: the extractors read 23 named tshark fields, and a renamed field does not raise — it silently yields an empty finding, i.e. absence scored as compliance, the one failure this project refuses to ship (T-024 already saw ADDKE naming move upstream). Added `preflight()` (verifies all 23 against `tshark -G fields`, 0.37s, cached once per process, `TUNNELSCOPE_SKIP_PREFLIGHT=1` to skip) + a `tunnelscope doctor` command. **(3) CLI could not signal findings**: every `cmd_*` returned `None` so `sys.exit()` always saw 0, defeating the role-D monitoring gate the ops plan promises. Added exit codes (0 clean / 1 findings / 2 input / 3 dependency) behind an **opt-in** `--fail-on-findings` so documented demo behaviour is unchanged; on `fleet` it also trips on captures that failed to parse, since a file never read has not been cleared. **(4) Raw tracebacks + swallowed cause**: `check=True` buried tshark's stderr inside `CalledProcessError`, so an operator saw the full command line but never *why*; now surfaced ("isn't a capture file in a format TShark understands"). **(5) No subprocess timeout** — one pathological capture could hang an entire fleet scan; bounded by `TUNNELSCOPE_TSHARK_TIMEOUT` (120s). **(6) Latent silent-loss bug found while fixing (5)**: `ike_sa_crypto()` parsed ISAKMP flags with `_int()`, which reads a bare `20` as decimal 0x14 — the responder bit would read clear, the function would return `{}`, and the whole IKE crypto finding would vanish with no error. tshark 4.2.2 emits `0x`-prefixed flags so it is not live today, but it was one upstream formatting change away; both call sites now share one `_flags()` hex reader. **(7) Double directory walk** introduced and removed during the work: `render`/`render_json` now accept a pre-computed scan so the CLI walks captures once. Also: dashboard CI job added (typecheck + lint + build — the dashboard had **no** automated check at all despite shipping real code), dead `json` import dropped, file writes use context managers. 67/67 unit tests (was 52), 69/69 e2e, dataset PASS, verified on a real tshark 4.2.2 | `tunnelscope/errors.py` (new), `tunnelscope/ingest/tshark.py`, `tunnelscope/report/fleet.py`, `tunnelscope/cli.py`, `tests/test_robustness.py` (new, 15 tests), `.github/workflows/ci.yml`, `tunnelscope/README.md` |
| T-065 | 5 real-browser polish fixes from user review at full screen width (not caught by the narrow Claude Browser pane): (1) `SeverityBars` value labels overflowed the card past ~1200px — fixed with proportional `XAxis` domain headroom (`max*1.25` vs a flat `+1`) plus wider right margin, verified via DOM `getBoundingClientRect` at a real 1600px viewport (all three labels now sit inside the card, negative overflow); (2) `SignalTrace` reveal was too fast — `DRAW_MS` 900→1800ms; (3) its stairstep look ("too many sharp edges") — `Area` `type="step"`→`"monotone"`, confirmed via the rendered path's `d` attribute now using Bezier `C` commands, not straight `L` segments; (4) wordmark too small — `Topbar`'s `Wordmark` `text-[22px]`→`text-[34px]`; (5) removed the "Fleet" pill next to the wordmark entirely (user: not wanted). `tsc -b`, `npm run build`, `npm run lint` all clean (no new warnings) | `fleet-dashboard/src/components/dashboard/{SeverityBars,SignalTrace,Topbar}.tsx`, `fleet-dashboard/.claude/launch.json` (new, for local dev-server preview) |
| T-064 | Unblocked T-063: user supplied the licensed `KuficaBold.woff2` directly (`/Users/bala/Downloads/`), resolving the network-egress block. Wired via `@font-face` in `index.css`, `--font-display` repointed, Bebas Neue package removed. Then: properly animated all three shadcn/Recharts charts (previously `isAnimationActive={false}` everywhere — a defensive move from headless-screenshot artifacts, not a real rendering issue). `SignalTrace` (Area): real 900ms reveal via Recharts' own clip-path, verified directly via DOM instrumentation (clip width tracked 0→9.7→165→646→complete on schedule) since screenshots proved unreliable for catching the exact frame; custom posture dots pop in staggered, timed to land as the line reaches each one. `PostureGauge` (Pie donut): 700ms native sweep, center "%" label and legend rows fade in after the sweep completes, not simultaneously. `SeverityBars` (Bar): 650ms native grow, value labels land at 75% of the grow, not on load. All three animations gated on a new `usePrefersReducedMotion` hook (Recharts' JS-driven animation isn't reachable by the existing CSS reduced-motion rule, which only handles CSS animation/transition). `npm run build` and `npm run lint` both clean (4 pre-existing warnings, none new); no console errors on a clean load | `fleet-dashboard/src/index.css`, `fleet-dashboard/src/lib/use-reduced-motion.ts`, `fleet-dashboard/src/components/dashboard/{SignalTrace,PostureGauge,SeverityBars}.tsx`, `fleet-dashboard/src/assets/fonts/KuficaBold.woff2`, `fleet-dashboard/README.md` |
| T-062 | *(committed as T-051 on GitHub `main`, renumbered at the 2026-09-18 merge; T-063…T-067 likewise)* Fleet dashboard visual redesign (user request, screenshot reference): black/violet/silver-white base with green/yellow/red used strictly as the status-indicator vocabulary (posture chips, severity labels, chart colors), replacing the earlier teal/steel palette everywhere (`index.css` tokens, `lib/fleet.ts`, all `dashboard/*` components). Removed the generic geometric-SVG header logo entirely — the wordmark is now the logo: "Tunnel" in Geist, "Scope" in **Bungee** (Google Fonts, OFL). The user's reference image named the commercial font **Kufica** (Artegra/Creative Fabrica) explicitly; checked it has no free-license distribution (only piracy mirrors), so it was not downloaded or bundled — Bungee substitutes as a same-spirit bold geometric display face, with the swap-in path for a licensed Kufica documented in `README.md` and `index.css`. All card-level containers bumped to a visibly rounded `rounded-2xl`; radius tokens increased project-wide. Fixed real overflow risks found while verifying: KPI card label/value truncation with `title` tooltips, the expanded verdict rows' rule-id/baseline columns (`truncate` + flexible description column instead of an unconstrained fixed-width span), and the signal-trace chart's x-axis city labels, which visibly overlapped at mobile width (390px) — fixed with rotated ticks and a wider left margin, verified with Playwright screenshots at both 1400px and 390px before and after. Verified via `tsc -b`, `npm run build`, and a running dev server (`npm run dev`), not just visual inspection | `fleet-dashboard/src/index.css`, `fleet-dashboard/src/App.tsx`, `fleet-dashboard/src/lib/fleet.ts`, `fleet-dashboard/src/components/dashboard/{Topbar,KpiStrip,FleetRegister,SignalTrace,SeverityBars}.tsx`, `fleet-dashboard/README.md`, `fleet-dashboard/package.json` (`@fontsource/bungee` added) |
| T-050 | Local fleet dashboard (user request: "how users use it", then a premium/animated UI, then clean minimal, then shadcn for the graphs). A real **Vite + React + TS** app in `fleet-dashboard/` (NOT an artifact) fed by real `tunnelscope fleet --json` over 10 validated captures (`src/gateways.json`). Design iterated on feedback: rejected the AI-slop violet + generic SVG (researched the tells), then an amber "instrument" look (user: gold looks cheap), landing on **clean-minimal cool-graphite + single teal accent, Geist superfamily, geometric tunnel-aperture logo** — grounded via the `frontend-design` + `artifact-design` + `dataviz` skills (one bold data-driven hero headline, de-chromed the eyebrow/middle-dot tells). All three graphs are **shadcn chart components** (Recharts): signal-trace `AreaChart`, posture `PieChart` donut, severity `BarChart`. Fixed a real Recharts+Vite duplicate-React crash (`resolve.dedupe` + `optimizeDeps`). `theme-factory` comparison of 6 palettes rendered for the user (choice pending). Type-checks clean, no console errors on fresh load | `fleet-dashboard/` (README documents the design rationale), `fleet-dashboard/src/components/dashboard/*`, `fleet-dashboard/src/components/ui/chart.tsx`, `.claude/launch.json` |
| T-047 | EXP-10 vendor-diversity via free/open-source alternative (user request). OpenIKED/OpenIKEv2/racoon2 each ruled out with evidence (dead/archived/unstable). Built a real OpenBSD 7.9 arm64 VM (Parallels, native, isolated network) running genuine `iked`; strongSwan 6.1.0 (macOS host) negotiated against it — a real, architecturally independent third codebase. `ike_meta`/`ike_crypto`/PQ-posture and EXP-06 failure-diagnosis all generalized correctly on a real auth failure (byte-exact to `iked`'s own log); **also found a genuine gap**: the same heuristic misdiagnosed a real success as rejected (no ESP sent) — reported honestly, not hidden. Also surfaced OpenBSD's own PQ mechanism (`sntrup761x25519`) our detector doesn't recognize (scoped follow-up). **Addendum:** pushed into the ESP dataplane too — real ICMP through the tunnel confirmed macOS's kernel *does* enforce the SA (the route warning is non-fatal) and surfaced a second genuine gap: the TFC-padding heuristic false-positives on naturally-uniform traffic. **Both gaps since fixed honestly** (not curve-fit): failure-diagnosis now reports `post-auth-outcome-ambiguous` at confidence 0.4 instead of a specific wrong answer at 0.7; TFC heuristic now requires size ≥1200B (grounded in RFC 4303's pad-to-MTU), not just uniformity. 75 pcaps, dataset validator PASS, e2e still 69/69, 41/41 unit tests | `experiments/exp10-openbsd-iked-generalization/RESULT.md`, `testbed/captures/exp10/` (2 pcaps + 2 groundtruth.json), `research/registers/EXPERIMENT-REGISTER.md`, `tunnelscope/evidence/extract.py`, `tunnelscope/leakage/leakage.py`, `tests/test_extract.py`, `tests/test_leakage.py` |
| T-049 | Usage & operations plan (user request) + immediate execution. `build/03-USAGE-AND-OPERATIONS-PLAN.md`: per-role workflows (grounded in `research/03`'s 4 roles, not invented), deployment model (I9-consistent, offline by default), real team ownership, explicit non-goals (public API deferred pending security review). Built the plan's top items same session: `tunnelscope fleet` (many-captures-in, one-view-out, per-baseline FAIL rollup never a blended score, errors surfaced never dropped), CI (unit+e2e+dataset checks on every push), dependency cleanup (removed unused fastapi/uvicorn that contradicted I9), `CHANGELOG.md` started, version 0.1.0→0.2.0. 52/52 unit tests, e2e still 69/69, dataset validator PASS (78 pcaps) | `build/03-USAGE-AND-OPERATIONS-PLAN.md`, `tunnelscope/report/fleet.py`, `.github/workflows/ci.yml`, `CHANGELOG.md`, `pyproject.toml`, `tests/test_fleet.py` |
| T-048 | Full-project audit (user: "complete the entire project completely... make an audit"). Checked every PS requirement (R1-R17), every register's stated status, and the architecture doc's own planned components against actual code — not just the task tracker. Found and closed 3 real gaps (EXP-11 auth-method inference, corrected R7; SPI exposed as a Finding, R9; EXP-12 rekey-cadence measurement, R9/R12 — which itself found and fixed a real false-positive bug in the shipped CVE-2026-78135 detector) and 2 stale docs (09-DEFINE.md R16's conformal-prediction claim corrected; OPEN-QUESTIONS.md fully rebuilt from a confusing append-only history to one accurate row per question, with OQ-14 checked live against the portal). 3 items left open by informed choice (OQ-15/22/23, all low-stakes, stated why). 48/48 unit tests, e2e still 69/69, dataset validator PASS (78 pcaps) | `experiments/exp11-auth-method-inference/RESULT.md`, `experiments/exp12-rekey-cadence/RESULT.md`, `research/09-DEFINE.md`, `research/registers/OPEN-QUESTIONS.md`, `research/registers/EXPERIMENT-REGISTER.md`, `tunnelscope/evidence/extract.py`, `tunnelscope/ingest/tshark.py` |
| T-001 | Research plan and methodology | `research/00-RESEARCH-PLAN.md` |
| T-002 | Discover phase D1–D8 | `research/01`–`08-*.md` |
| T-003 | Define phase | `research/09-DEFINE.md` |
| T-004 | Research exit review → freeze | `research/10-RESEARCH-EXIT-REVIEW.md` |
| T-005 | Docker testbed (classical + PQ strongSwan 6.0.2, keyless router vantage) | `testbed/`, commit `6c41332` |
| T-006 | EXP-02 negative control — PASS | `experiments/exp02-negative-control/results/` |
| T-007 | EXP-01 cipher sieve — partially falsified, corrected | `experiments/exp01-cipher-sieve/results/`, DEC-013 |
| T-008 | EXP-03 PFS signature — confirmed | `experiments/exp03-pfs-signature/results/` |
| T-009 | EXP-04 PQ observability — confirmed, 4 signals | `experiments/exp04-pq-length-asymmetry/results/` |
| T-020 | Existing-solutions deep dive: problem restated, 20+ solutions (mechanism, build, maintenance, PS coverage, gap), competing SIH repos, synthesis | `research/11-EXISTING-SOLUTIONS-DEEP-DIVE.md` |
| T-020a | Live maintenance metrics for 18 repos | `research/data/solutions-maintenance-2026-09-11.json` |
| T-020b | Source-level RFC 9370 check → Wireshark YES (fixed 2026-03-14); Zeek, Suricata, nDPI NO; Arkime no ADDKE found. Closes OQ-26/27 | Doc 11 §3.A, RL-029/030 |
| T-020c | Commercial survey → AQtive Guard TLS/SSH only; Palo Alto own tunnels only (third-party UNK); Greenbone IKEv1-only (checked on the local feed) | Doc 11 §3.B/3.D, RL-031/032 |
| T-020d | Competing projects → a PS-26160 repo with synthetic-Gaussian ML and fabricated defaults; a strong but confounded dataset lab | Doc 11 §3.F, RL-034 |
| T-020e | PS A–E coverage matrix | Doc 11 §5 |
| T-020f | Synthesis + corrections (DEC-016, DEC-017) | Doc 11 §6–7 |
| T-020g | Registers updated; correction banner on doc 07; README fixed; committed and pushed | `research/registers/*`, doc 07, commit `c39dc60` |
| T-016 | Fixed `datetime.utcnow()` deprecation in `run_arm.sh` (already correct in `run_pq_arm.sh`); confirmed no other occurrences in testbed/ or experiments/ | `testbed/scripts/run_arm.sh` |
| T-021 | Upgraded PQ lab image 6.0.2 → 6.1.0 (fixes CVE-2026-78133, potential RCE). Clean rebuild, no plugin regressions. Reran EXP-04: all 4 signals byte-for-byte identical to 6.0.2 | `testbed/images/strongswan-pq/Dockerfile`, `testbed/NOTES.md` #12, `experiments/exp04-pq-length-asymmetry/results/exp04_6.1.0_confirmation.md` |
| T-010 | EXP-06 round 2 — all 6 pre-registered predictions held; rules = decision tree (macro-F1 1.000 with F2/F3 merged); F2 vs F3 provably inseparable passively; **CS-02 needs no ML** (DEC-018) | `experiments/exp06-failure-diagnosis/RESULT_R2.md`, `results/exp06r2_results.json`, 35 captures in `testbed/captures/exp06r2/` |
| T-017 | PS ID confirmed **SIH26160** (NTRO, "AI-Powered IPsec VPN Protocol Analyzer…"); SIH26161 is an unrelated NTRO dam-break statement. Earlier doubt came from a search-engine summary | Raw dataset `NoBugNinja/Smart-India-Hackathon-SIH-2026-Problem-Statements/data/sih2026_ps_20260822_211225.json` |
| T-012 | EXP-05 metadata leakage — all 5 pre-registered predictions held; TFC padding zeroes size leakage (+54% bytes) yet class inference stays at F1 0.995; ML justified only as a measuring instrument; 2 analysis bugs caught | `experiments/exp05-metadata-leakage/RESULT.md` |
| T-011 | EXP-07 Libreswan 5.4 — 7/7 predictions held; protocol facts generalise; notify + fragmentation implementation-dependent (DEC-020) | `experiments/exp07-libreswan-generalization/RESULT.md` |
| T-014 | 09-DEFINE updated with EXP-01/03/04/05/06/07 outcomes; 8 of 13 capability rows now deterministic | `research/09-DEFINE.md` |
| T-015 | **DEVELOP / GATE-5** — 8 concepts + 2 composites; weights committed before scoring; K1 selected (wins 22/23 weighting scenarios); red team; staged build order (DEC-023) | `research/12-DEVELOP.md`, `research/data/develop_*.json` |
| T-043 | User request: finish every pre-build item before building — all done | commits `982da43` → `32ca480` |
| T-044 | EXP-08 mode inference — **NOT-OBSERVABLE at T0**: fixed +20 B offset only with a paired baseline; without one every ESP length is valid in both modes. Resolves the last candidate-ML row → only CS-01 leakage remains ML (DEC-024) | `experiments/exp08-mode-inference/RESULT.md` |
| T-013 | EXP-04 follow-up — reassembled the fragmented IKE_INTERMEDIATE: initiator KE plaintext 1216 B vs responder 1112 B = **104 B asymmetry**, matching ML-KEM-768 ek(1184)−ct(1088)=96 B. PQ-6 confirmed as secondary signal | `experiments/exp04-pq-length-asymmetry/reassemble_ke.py` + `results/t013_ke_asymmetry.json` |
| T-024 | EXP-04 oracle — **four independent sources** agree ADDKE1 ID 36 = ML-KEM-768: IANA registry (RFC-ietf-ipsecme-ikev2-mlkem-09), Wireshark master `packet-ike.c`, tshark 4.6.4/4.6.8 parsing our capture, strongSwan T2 log | this table; IANA + Wireshark master verified |
| T-022 | EXP-09 CVE-2026-78135 detector — **shipped in the package** (`extract_early_childsa_cve` + `rules/cve-2026-78135.yaml`), deterministic + vantage-aware. Three validation layers: specificity 0 FP/69, sensitivity 1/1 on a synthetic plaintext-structural positive, AND sensitivity confirmed on a **genuine live fault-injected exploit** (real strongSwan 6.1.0 traffic, two one-line build-asserted patches, isolated Docker lab, root-cause gate bypass independently confirmed via the daemon's own debug log). OQ-31 closed | `experiments/exp09-early-childsa-cve/RESULT.md`, `tests/test_cve.py` (7), `testbed/captures/{synthetic,exploitlab}/`, `testbed/images/strongswan-exploitlab/`, `testbed/docker-compose.exploitlab.yml` |
| T-045 | Stage-3 C5 cross-tier consistency — `tunnelscope/crosstier/` reconciles T2 endpoint telemetry vs T0/T1 findings: escalation (resolves NOT-OBSERVABLE mode), confirmation/refinement (ESP cipher family→exact), CONTRADICTORY (the real NOTES #13 IP-TFS case). CLI `crosstier`. Trust stays causal (T2 is ground truth). 8 tests | `tunnelscope/crosstier/crosstier.py`, `build/02-CROSSTIER.md`, `tests/test_crosstier.py`, `testbed/telemetry/` |
| T-023 | Dataset hygiene — `dataset/build_manifest.py` (69 pcaps, per-file SHA-256, causal T2 ground truth, vantage, train/val/locked-test split by session/config) + `dataset/DATASHEET.md` + strict `dataset/validate.py` (exits non-zero on hash/provenance/leakage violations; **PASS**). Credits `naman9271/ipsec-pcap-lab` | `dataset/` |
| T-025 | OQ-30 closed — Palo Alto Quantum Readiness inventories TLS/SSH via decryption logs + its OWN VPN tunnels; third-party IPsec merely transiting is NOT inventoried. The doc-11 assessment gap stands | `research/11-...md`, OQ-30 |
| T-030 | System architecture — `build/00-ARCHITECTURE.md`: components, data flow, 9 design invariants, requirement traceability, 6 ADRs, staged build order, testing strategy | `build/00-ARCHITECTURE.md` |
| T-031 | Evidence extraction layer — `tunnelscope/` package: tshark ingest (reuse, ADR-001), Finding/EvidenceRecord core with mandatory status (ADR-002), 5 extractors (ike_meta, pq_addke, pfs, mode, failure) + CLI. Reproduces EXP-03/04/06/08 on BOTH implementations; 9 tests vs ground truth pass | `tunnelscope/`, `tests/test_extract.py` |
| T-032 | Deterministic assessment engine — `tunnelscope/assess/engine.py` + 3 versioned baseline files (`rules/`: DISA VPN SRG, RFC 8247, DST/NQM). Verdicts PASS/FAIL/UNKNOWN/NOT-OBSERVABLE/CONTRADICTORY, each citing authority+rule+evidence. Demonstrates the MODP-2048 multi-baseline split (PASS RFC 8247 / FAIL DISA). 4 tests incl. zero-false-PASS pass. Added `ike_crypto` + `cipher_sieve` extractors | `tunnelscope/assess/`, `rules/`, `tests/test_assess.py` |
| T-033 | PQ posture + downgrade assessor + CBOM (CS-05, DEC-017) — `tunnelscope/pq/cbom.py` emits CycloneDX 1.6. Full chain proven on a dedicated **downgrade arm** (`pq-downgrade.pcap`: ML-KEM offered, MODP-2048 selected, 0 IKE_INTERMEDIATE): extractor→`offered-but-not-used`, DST-PQ-DOWNGRADE→FAIL, CBOM posture→DOWNGRADED. CBOM never overstates (gaps recorded). 4 tests | `tunnelscope/pq/cbom.py`, `tests/test_pq_cbom.py`, `testbed/captures/pq-downgrade.pcap` |
| T-035 | Security score — `tunnelscope/score/score.py` + `build/01-SCORING-METHODOLOGY.md`. Per-baseline (never one number), severity-weighted pass rate over ASSESSABLE rules, coverage reported alongside, sensitivity-tested (stable/fragile). Returns None (not a fake score) when nothing is assessable. 3 tests | `tunnelscope/score/`, `build/01-SCORING-METHODOLOGY.md` |
| T-036 | Reports — `tunnelscope/report/report.py`: executive + technical from the evidence graph (I8). Every finding tagged observed/inferred/not-observable; every verdict cites its authority; multi-baseline scores + fragility note; downgrade + high-severity surfaced. CLI `report`. 2 tests | `tunnelscope/report/`, `tests/test_report.py` |
| T-034 | Leakage-measurement module (CS-01, Stage 2) — `tunnelscope/leakage/leakage.py`: per-capture size/timing entropy in bits + TFC-padding detection, wired into the pipeline. Verified: unpadded leaks size+timing; TFC-padded → size 0 bits, timing remains (EXP-05). Reports bits, never a traffic label (DEC-021). Also fixed ESP-only flow handling (T0 forensic). 3 tests | `tunnelscope/leakage/`, `tests/test_leakage.py` |
| T-039 | End-to-end validation — `build/validate_e2e.py`: runs the full pipeline over all 69 dataset captures, checks every finding vs causal ground truth. **69/69 pass, 0 mismatches**, both implementations; standing anti-overclaim checks (mode never valued at T0; no ESP key length) enforced. Exits non-zero on any mismatch | `build/validate_e2e.py`, `build/E2E-VALIDATION.md` |
| T-041 | Technical documentation — `tunnelscope/README.md` (install, usage, design, the one ML component, trust/limits, tests) | `tunnelscope/README.md` |
| T-037 | Analyst dashboard — `tunnelscope/report/dashboard.py`: self-contained, offline HTML (ADR-006), theme-aware, status-colour-coded (observed/inferred/not-observable/contradictory distinct, DEC-008). Per-SA posture badge, per-baseline scores+coverage, fragility note, verdict + evidence tables, "not observable" section. CLI `dashboard`. Rendered + verified | `tunnelscope/report/dashboard.py`, `build/demo/` |
| T-009b | EXP-06 round 1 — inconclusive, root cause documented | `experiments/exp06-failure-diagnosis/RESULT.md`, commit `27a136d` |
| T-042 | Project named **TunnelScope** (user choice, 2026-09-11); GitHub repo renamed `7-Bala/SIH26` → `7-Bala/TunnelScope`, local remote updated, top-level README added | `README.md`, `gh repo view 7-Bala/TunnelScope`, this commit |

---

## Changelog

- **2026-09-11** — Created this file (user request). Backfilled done work from commits `e92defe`,
  `6c41332`, `27a136d`. Opened T-020 (existing-solutions deep dive) as current focus.
- **2026-09-11** — T-020 done. Main outcomes: (1) **our claim "Wireshark can't decode PQ IKE" was
  wrong** — the issue was fixed 2026-03-14 — withdrawn, and CS-05 restated (DEC-017); (2) the gap is
  confirmed in *assessment* (PS columns D/E), not parsing; (3) an actual competing PS-26160 repo was
  found and read. New: T-021–T-025. Still carried over: T-010–T-017.
- **2026-09-11** — User asked "what's left". Added the roadmap to submission (T-030–T-041), covering
  every PS deliverable not yet started. Recorded the unknown SIH deadline under Blocked.
- **2026-09-11** — Project named **TunnelScope** (user's choice, from a list of Tunnel-themed
  options). GitHub repo renamed `SIH26` → `TunnelScope`; local remote updated; top-level
  `README.md` added.
- **2026-09-15** — T-065 done: user reviewed the fleet dashboard in a real full-screen Chrome
  window (not the narrow Claude Browser pane used for prior verification) and reported 5 concrete
  issues — severity-bar numbers overflowing their card, the signal-trace reveal too fast, its
  stairstep shape too sharp-edged, the wordmark too small, and an unwanted "Fleet" badge. All 5
  fixed and verified via DOM inspection at a genuine 1600px viewport, the exact condition the
  overflow was reported under.
- **2026-09-11** — User asked directly whether planning is over. Honest answer: **no**. T-015
  (DEVELOP/GATE-5) never ran, so no concept was ever formally selected; 3 of 8 experiments are
  unstarted and EXP-06 is inconclusive, leaving 2 of 3 candidate ML components unvalidated and all
  findings strongSwan-only. Raised T-014 and T-015 to P0 and marked the gate to building above.
  Counter-risk noted: over-planning is now the bigger danger than under-planning.
- **2026-09-11** — User: finish everything pre-build before building. Opened T-043 (umbrella). Raised
  T-010/T-011/T-012/T-016/T-017/T-021 to P0 alongside T-014/T-015. Started T-021 + T-016.
- **2026-09-12** — T-016 and T-021 done (evidence above). EXP-04 reconfirmed on strongSwan 6.1.0,
  identical signals — the finding was not a build artifact. Moving to T-010 (EXP-06 round 2).
- **2026-09-12** — T-010 done: EXP-06 r2 matched all six pre-registered predictions; failure
  diagnosis is deterministic (DEC-018). T-017 done: SIH26160 confirmed. T-014 mostly done (PFS and
  failure diagnosis both moved from "ML" to deterministic, DEC-019). Found that Docker's kernel lacks
  IP-TFS (NOTES.md #13) — EXP-05's IP-TFS arm dropped; EXP-05 now running. Libreswan 5.4 (PQ-capable)
  located in Fedora rawhide for EXP-07.
- **2026-09-12** — EXP-05 captures running (27/52); a dry run caught an analysis bug (direction
  leaking into the "size" MI) — fixed before the final analysis, and logged in the code. Added a
  depth-2 tree baseline for the AI-necessity question. EXP-07 queued behind EXP-05. DEVELOP weights
  committed before scoring.
- **2026-09-12** — **T-043 complete: planning finished.** EXP-05 and EXP-07 done; T-014 done;
  T-015 DEVELOP done: K1 selected (DEC-023). Only non-gating items remain (T-013, T-022–T-025,
  T-044). Next: T-030 architecture. Still blocked on the user: the SIH deadline.
- **2026-09-12** — User: finish all processes before building. Closed T-044 (mode NOT-OBSERVABLE at
  T0 — last ML row resolved), T-013 (KE asymmetry 104 B ≈ 96 B theoretical), T-024 (4-source ML-KEM
  oracle), T-022/EXP-09 (CVE detector, 0 FP, vantage-aware). Remaining: T-023, T-025.
- **2026-09-12** — T-023 (dataset manifest + datasheet + strict validator, PASS) and T-025 (Palo
  Alto does NOT assess transiting third-party IPsec) done. **All pre-build processes finished.**
  Next: T-030 architecture.
- **2026-09-12** — User: no deadline → full staged build (DEC-023). Blocker cleared. Starting the
  build phase with T-030 (system architecture) as the first act of building.
- **2026-09-12** — BUILD started. T-030 architecture (9 invariants, 6 ADRs, traceability). T-031
  evidence extraction: tunnelscope package works end-to-end, reproduces every experiment finding on
  strongSwan AND Libreswan captures, 9 ground-truth tests pass. Next: T-032 assessment engine.
- **2026-09-12** — T-032 assessment engine done: rules as versioned YAML, multi-baseline verdicts
  with citations, zero-false-PASS validated, the MODP-2048 RFC-vs-DISA contradiction shown live.
  13 tests pass. Building T-033 (PQ assessor + CBOM) next.
- **2026-09-12** — T-033 done: PQ downgrade detection + CycloneDX CBOM export, proven end-to-end on
  a dedicated downgrade arm. 16 tests pass; dataset validator still PASS. Next: T-035 score, T-036 reports.
- **2026-09-12** — T-035 score (per-baseline, coverage-aware, sensitivity-tested) and T-036 reports
  (exec + technical from evidence graph) done. 21 tests pass. Stage 1 MVP is essentially complete
  (ingest→evidence→assess→score→report→CBOM, all deterministic). Next: T-034 leakage module (Stage 2).
- **2026-09-12** — T-034 leakage module done (Stage 2): size/timing bits + padding detection wired
  into the pipeline; ESP-only forensic captures now produce records. 24 tests pass. Next: T-037
  dashboard (Stage 3), T-039 validation, T-041 docs.
- **2026-09-12** — T-039 end-to-end validation: 69/69 captures match ground truth, zero overclaims.
  T-041 tool README. Next: T-037 dashboard, T-038 dataset release packaging, T-040 pitch/Q&A.
- **2026-09-12** — T-037 dashboard: offline self-contained HTML, rendered and sent. All build
  components now exist (ingest→evidence→assess→score→report→CBOM→dashboard). Remaining: T-038
  dataset packaging, T-040 pitch/Q&A prep (demo video needs the user to record).
- **2026-09-12** — T-038 dataset release (README + datasheet + hash-verified manifest, validator
  PASS) and T-040 SIH materials (pitch deck, jury Q&A, demo script — commands verified against real
  captures) both DONE. **Build roadmap T-030–T-041 complete.** Remaining are the documented
  deferrals only: T-022 true-positive CVE arm (needs a malicious IKE stack) and the optional Stage-3
  C5 endpoint cross-check. The demo *video* is the user's to record from `build/sih/DEMO-SCRIPT.md`.
- **2026-09-12** — User: "complete everything left behind except the demo video." Closed both:
  **T-022** — wired the CVE-2026-78135 detector into the package as a real extractor + `CVE-WATCH`
  rule, and validated SENSITIVITY with a synthetic plaintext-structural positive (0 FP/69, 1/1 TP).
  **T-045** — built the optional Stage-3 C5 cross-tier module (T2-vs-wire reconciliation, incl. the
  NOTES #13 IP-TFS CONTRADICTORY). 38 tests pass (was 24), e2e still 69/69, dataset validator PASS
  (72 pcaps, synthetic fixture excluded from all ML splits). **Only the demo video now remains, and
  it is the user's to record.** The one stated out-of-scope gap is a live-crypto CVE exploit capture.
- **2026-09-12** — User: attempt both remaining stretch items (live CVE exploit, vendor-appliance
  validation). **live CVE exploit** — first pass cited the root-cause gate but stopped short
  (commit `fe52c31`); user pushed to finish it, so continued: mapped `initiate_tasks()`'s
  exchange-selection (picks exchange from the first recognized queued task type, independent of
  IKE_SA state), wrote two one-line, build-asserted patches (`testbed/images/strongswan-exploitlab/`:
  attacker skips activating `TASK_IKE_AUTH`; vulnerable relaxes the `reject_request()` gate), built
  an isolated Docker lab (`testbed/docker-compose.exploitlab.yml`, separate network from the
  validated testbed), and captured a **genuine live exchange**: real strongSwan 6.1.0 traffic
  showing `IKE_SA_INIT → CREATE_CHILD_SA` with zero `IKE_AUTH`. TunnelScope's detector fires FAIL
  (high) on it — same result as the synthetic positive, now on real traffic. Gate bypass
  independently confirmed via the responder's own cfg-debug log (reached TS evaluation, past the
  state check). Honestly documented what did NOT happen too: no Child SA installed (T2: empty on
  both sides), root cause diagnosed (responder has no linked child_cfg without IKE_AUTH's identity
  selection) rather than left unexplained. **T-022 now genuinely complete at the wire-detection
  level** (`experiments/exp09-.../RESULT.md`, `tests/test_cve.py` — 7 tests). **Vendor-appliance
  validation (Cisco/Palo Alto/Fortinet)** — still genuinely blocked: needs licensed vendor VM images
  or hardware I have no way to obtain; not attempted because there is no legitimate path to do so
  without the user supplying access.
- **2026-09-13** — User: for vendor-diversity, "see for any other free or open source alternative
  and use it." Researched real alternatives (not assumed): OpenIKED's Linux port explicitly dropped
  (confirmed from its own commit history), OpenIKEv2 archived since 2020, racoon2 self-described
  unstable/abandoned ~15y — all ruled out with evidence, none faked as viable. Built a genuine
  OpenBSD 7.9 arm64 VM (Parallels Desktop, native — no emulation, this Mac is Apple Silicon) running
  real `iked`, on an isolated Host-Only network never touching the validated Docker testbed.
  strongSwan 6.1.0 (installed via Homebrew on the macOS host) negotiated against it — first real
  cross-check against a codebase outside the strongSwan/Libreswan lineage. User ran the one
  root-requiring step themselves (tcpdump capture) since I do not enter sudo passwords. Result
  (T-047/EXP-10): core deterministic extractors generalized correctly, including a byte-exact
  failure diagnosis on a real auth error — but also caught a genuine false negative (a real success
  misdiagnosed as rejected, because no ESP traffic had been sent) and a real generalization limit
  (OpenBSD's own PQ mechanism, `sntrup761x25519`, isn't recognized by our ADDKE-based detector).
  Both reported honestly, neither hidden. 74 pcaps, e2e still 69/69, dataset validator PASS.
- **2026-09-13** — User: "proceed with next." Pushed EXP-10 further to cover the ESP dataplane
  (the first run had 0 ESP packets). VM had gone to a stale suspended snapshot between sessions;
  fixed with `prlctl reset` for a genuine cold boot. Re-established the tunnel, sent real ICMP
  through it, captured on the same T0 vantage. **Two more honest findings:** (1) charon's
  `[KNL] can't install route ... conflicts with IKE traffic` warning is NON-FATAL — macOS's kernel
  did enforce the SA; all 10 packets were genuine ESP with the correct SPIs (T2-confirmed via
  OpenBSD's `ipsecctl -sa`). (2) A **second real, previously-undisclosed gap**: the TFC-padding
  heuristic (`leakage.py`) false-positives on naturally-uniform traffic (5 identical-size ICMP
  probes triggered `tfc_padding_active: True` with no padding configured) — it cannot distinguish
  "uniform because padded" from "uniform because the traffic itself is uniform," a case EXP-05's
  synthetic dataset never tested. Not fixed this session; recorded as a follow-up, same as finding
  1 from the earlier run. 75 pcaps, e2e still 69/69, 39/39 unit tests, dataset validator PASS.
  `experiments/exp10-openbsd-iked-generalization/RESULT.md` (addendum), `testbed/captures/exp10/`.
- **2026-09-13** — User: "complete all." Fixed both EXP-10 gaps — honestly, not by curve-fitting.
  **Failure-diagnosis:** first instinct (raise the byte threshold) was tried and empirically
  falsified before shipping: strongSwan's own F2/F3 rejections are 256B, but iked's real success is
  224B — smaller than strongSwan's own rejection, so no single constant classifies both. The honest
  fix: the ambiguous branch now reports `post-auth-outcome-ambiguous` (was the specific,
  sometimes-wrong `child-sa-rejected`) at confidence 0.4 (was 0.7), stating both live hypotheses.
  **TFC-padding heuristic:** grounded in the actual protocol concept — TFC pads to path MTU
  (RFC 4303 §2.7; EXP-05's own capture confirms 1472B on this network) — so "all one size" now also
  requires size ≥1200B before calling it padding. Verified against both real cases (112B uniform →
  now correctly False; 1472B MTU-padded → still correctly True). Both locked in with regression
  tests (`test_extract.py::test_failure_diag_admits_ambiguity_on_real_success_with_no_esp`,
  `test_leakage.py::test_naturally_uniform_traffic_is_not_tfc_padding`). 41/41 unit tests, e2e still
  69/69, dataset validator PASS.
- **2026-09-13** — User: "complete the entire project completely... make an audit what yet is
  left." Full audit against every PS requirement (R1-R17), every register's stated status, and the
  architecture doc's own planned components (not just the TODO list). Found 3 real gaps + 2 stale
  docs, all now closed:
  - **T-048/EXP-11 (R7/OQ-05, auth method)** — built `extract_auth_hint`, then empirically
    falsified the easy version before shipping (matches the project's own standing discipline):
    SIGNATURE_HASH_ALGORITHMS is unconditional (zero discriminating value); CERTREQ reflects the
    *responder's* fleet-wide cert policy, not this SA's method (differential test: same responder,
    PSK vs cert, both showed CERTREQ). Shipped honestly: `peer_auth_method` NOT_OBSERVABLE (same
    encryption-boundary pattern as mode/EXP-08), `responder_cert_capability` as the correctly-scoped
    real capability. `research/09-DEFINE.md` R7 corrected to match.
  - **R9 (SPI not exposed)** — trivial fix, `ike_spi` now a citable Finding.
  - **`research/09-DEFINE.md` R16** — corrected: conformal prediction was never implemented (rightly
    superseded once 8/9 capabilities turned out deterministic); doc now says so instead of claiming
    a mechanism that doesn't exist.
  - **`research/registers/OPEN-QUESTIONS.md`** — full close-out pass. Was genuinely stale (closures
    appended below instead of updating rows in place, making resolved questions look open). Rebuilt
    with one row per question; checked OQ-14 (SIH IP/licensing) live against the portal (none
    published — internal pre-screening layer, non-issue since no third-party GPL code is bundled).
    3 questions left genuinely, honestly open (OQ-15, OQ-22, OQ-23), all low-stakes.
  - **T-048/EXP-12 (R9/R12, SA-lifecycle/rekey-cadence)** — built `extract_sa_lifecycle`
    (`rekey_cadence`, MEASURED, never a claimed configured lifetime — F-02). Validated on a real
    capture with 7 rekeys (4 manual + 3 interleaved auto); measured intervals matched the manual 15s
    spacing almost exactly (15.09s, 15.09s). **This same real capture found a genuine security bug**
    in the already-shipped CVE-2026-78135 detector (T-022/EXP-09): message IDs are per-originator
    (RFC 7296 §2.1), so a responder-initiated rekey's own counter restarting at 0 produced a false
    positive when compared globally against the initiator's sequence. Fixed to compare by
    frame/capture order; re-verified 0 FP on all 69 real captures, both true positives (synthetic +
    live exploit) still fire correctly. Added as a permanent regression test. EXP-09's own RESULT.md
    updated to point at this.
  48/48 unit tests (was 39 at the start of this audit), e2e still 69/69, dataset validator PASS
  (78 pcaps, was 71). **Full-project audit complete: every PS requirement (R1-R17) is now either
  built, or an honestly-disclosed, stated limitation — nothing is silently missing.** Remaining,
  stated explicitly: OQ-15/OQ-22/OQ-23 (low-stakes, open by choice, see OPEN-QUESTIONS.md), the demo
  video (user's task), and vendor-appliance licensing (blocked on the user supplying access).
- **2026-09-14** — User: "start doing the work according to the plan and now itself plan how the
  users are gonna use it and how we will manage and build it accordingly." Wrote
  `build/03-USAGE-AND-OPERATIONS-PLAN.md`: per-role usage workflows grounded in `research/03`'s 4
  established roles (not invented personas), a deployment model consistent with I9 (offline by
  default), team ownership mapped to the real 6-person roster, and an honest list of what's
  deliberately NOT being built yet (a public API — needs its own security review first). Then
  executed the plan's highest-value, lowest-risk items immediately, same session:
  - **T-049: `tunnelscope fleet`** — the actual gap the plan found: roles B (auditor) and D (SOC
    analyst) both need "many tunnels, one view," which nothing before this served (every prior
    surface was one-pcap-at-a-time). Built `tunnelscope/report/fleet.py`: per-baseline FAIL-count
    rollup (never a blended fleet score — DEC-007 applied at fleet scale), every tunnel its own row,
    a file that fails to parse is its own error row, never silently dropped (verified with an actual
    corrupt file). New CLI: `tunnelscope fleet <dir> [-o out.html] [--json]`. 4 new tests.
  - **CI** (`.github/workflows/ci.yml`) — unit tests + 69-capture e2e + dataset validate, on every
    push/PR. Scoped honestly in the workflow's own comment: this would not have found EXP-10/11/12's
    3 bugs itself (those needed real infra CI can't invent), but every one of them is now a
    permanent regression test CI enforces going forward.
  - **Dependency cleanup**: removed `fastapi`/`uvicorn` — declared since early scaffolding, never
    used anywhere, and quietly contradicted I9. Added an explicit `dev` extra (`pytest`) instead of
    an undeclared global install. Version bumped 0.1.0 → 0.2.0.
  - **`CHANGELOG.md`** — started, so the project's history is readable without opening `TODO.md`.
  52/52 unit tests (was 48), e2e still 69/69, dataset validator PASS (78 pcaps, unchanged — fleet
  mode adds no new captures, it's a new view over existing ones).
- **2026-09-14** — User: build the fleet dashboard, then several rounds of design direction, then
  "use shadcn components for all the graphs," then "update all files, starting a new session."
  Delivered a real local **Vite + React + TS** app (`fleet-dashboard/`, run: `npm --prefix
  fleet-dashboard run dev` → http://localhost:5173) on real `tunnelscope fleet --json` data.
  Design pivoted honestly on feedback (AI-slop violet → amber instrument → **clean minimal teal on
  graphite, Geist, tunnel-aperture logo**), guided by the genuinely-installed design skills
  (`frontend-design`, `artifact-design`, `dataviz`, `theme-factory`) — was honest that the
  screenshot's 42-skill "pack" can't be installed from an image and used the real subset instead.
  All three graphs are now **shadcn/Recharts** components (Area/Pie/Bar); fixed a real Recharts+Vite
  duplicate-React "invalid hook call" via `resolve.dedupe`. Committed as **T-050**. Theme choice
  (6-option `theme-factory` comparison) still pending the user's pick. The Python tool, tests,
  dataset and research are unchanged this session — 52/52 unit tests, 69/69 e2e still hold.
- **2026-09-14** — T-062 wordmark iteration continued. User asked to make the wordmark letters
  thicker; swapped the display face from Bebas Neue to Anton (also Google Fonts/OFL) as a bolder
  stand-in. Before that landed, user shared a reference screenshot naming a specific font,
  **Goroda**, and asked to download and use it, noting it's free for personal use. Checked: true
  (designer Zakharov Ilya, free-for-personal-use license) — but every site hosting it
  (fontmeme.com, befonts.com, confirmed directly by `curl`, both 403) is blocked by this session's
  network egress policy, same pattern as Kufica and Dugas Pro earlier this session. This is a
  network restriction, not a licensing objection — Goroda's own license fits this project fine.
  Asked the user how to proceed (send the file / pick a closer free substitute / keep Anton); they
  chose to park it — opened **T-063** (Blocked) — and revert the in-progress Anton swap back to the
  last committed state (Bebas Neue). Reverted cleanly (`git checkout` on the 3 touched files,
  `npm install` to resync `node_modules` with the checked-in lockfile); working tree now matches
  the last pushed commit exactly, verified with `git status`.
- **2026-09-15** — User supplied `KuficaBold.woff2` directly (path given, verified as a genuine
  WOFF2 by `file`), unblocking the wordmark font swap; then asked to "animate all the graphs
  properly and correctly." Wired Kufica in via `@font-face`, confirmed loaded via
  `document.fonts` in the live browser before moving on. Re-enabled real animation on all three
  shadcn/Recharts charts (previously disabled defensively, not because they were broken) with a
  new `usePrefersReducedMotion` hook gating each, since Recharts' JS-driven draw-in animation
  isn't reachable by the project's existing CSS `prefers-reduced-motion` rule. Verified the signal
  trace's reveal via direct DOM/clip-path instrumentation (screenshots alone proved unreliable for
  catching an exact animation frame — timing artifacts, not real bugs, confirmed by checking the
  resting state twice); confirmed the donut and severity bars render correctly at rest with real
  data after a clean page load. `npm run build` + `npm run lint` clean, no console errors.
  Committed as **T-064**; closed the stale **T-063** (Goroda) as superseded by this. On branch
  `claude/last-commit-history-dald9g`.
- **2026-09-17** — User: the 5-model review-panel dossier. Asked me to act as final decision maker
  (adopt/reject/adapt) and write a master implementation plan, **no code this turn**. Opened T-051.
  Before planning, checked the dossier's premises against the repo: `extract.py` is 392 lines (the
  dossier assumed ~1500), and its T0–T4 tier numbering differs from our `Vantage` enum (T2/T4
  swapped in meaning), so adopting its wording as-is would silently change what the tiers mean.
  Re-ran the suites: 52/52 unit, 69/69 e2e. Launched a multi-agent audit (`wf_a8c780dc-41f`): 8
  claim clusters, each with an RFC/code/capture analyst and 2 adversarial reviewers, plus a
  completeness critic.
- **2026-09-17** — Completed **T-051**: adjudicated the 5-model review panel dossier (ChatGPT-1/2,
  Claude, Kimi, Gemini) across all 9 rounds and 8 clusters. Wrote comprehensive master implementation
  plan and audit document `build/04-IMPLEMENTATION-PLAN.md`. Implemented approved fixes across
  `tunnelscope/evidence/extract.py` and `tunnelscope/ingest/tshark.py`: handled PQ ADDKE Transform ID 0
  (`NONE`) per RFC 9370 §2.1 with calibrated downgrade confidence and isolated responder selection on multi-offer requests;
  enabled per-SA crypto extraction in `ike_sa_crypto` via normalized `ispi` filter; resolved ESP packet pooling across
  multiplexed SAs with chronological timestamp sorting and active session SPI tracking in `group_sas`; preserved unified
  bidirectional ESP-only tunnel records without flow fragmentation; unflattened proposals in `tshark.py` and matched initiator
  offerings to responder choices in `extract.py`; added legacy IKEv1 detection (RFC 8247 deprecation); grounded PFS in
  RFC 5903 §7 64-octet Group 19 and calibrated 3-tiered thresholding (Curve25519: 255B, ECP: 280B, MODP: 400B). Added 9 unit
  tests across all addressed areas. Full verification: 61/61 unit tests pass, 69/69 E2E validation pass, 78/78 dataset pcaps
  hash-verified clean.
- **2026-09-17** — User: review/audit/stress-test the `antigravity` branch (T-051 done by another team while I was away) and say whether to merge. Did T-052: suites confirmed, full 130-capture differential against `main`, 3 synthetic stress cases, RFC text checked. Found 2 genuine fixes and 5 real defects (one a new regression). Recommendation: don't merge yet; fix list is T-053. Note: I made this edit on the `antigravity` working tree, uncommitted.
- **2026-09-17** — User accepted the T-052 verdict, decided ESP-only SPI changes count as one tunnel, and green-lit T-053 including commit + merge. Done: all 5 fix areas, plan rewritten from verified sources, 130-capture differential shows only intended changes, suites green (69 unit / 69 E2E / dataset PASS). Committed on `antigravity`, merged to `main` (local; not pushed). Next per plan §5 P1: script the differential into CI, CVE message-ID guard, jury wording for 'downgrade'/'matches CVE', UDP-encap/IPv6 length offsets, stats wording.
- **2026-09-17** — User re-verified `main` (69/69 unit, 69/69 E2E, dataset PASS, clean tree), adopted the T-046 numbers and PQ wording, staged plan §5 P1 as post-submission work, and asked for a final TODO sync. Done: Current focus rewritten to the real state (it still said T-030 was next); T-046 lists its remaining steps, incl. the one jury overclaim left (`PITCH-DECK.md:87`, found by grep); P1 items opened as T-054–T-058; T-051 row marked partly superseded; broken fragment in the roadmap table removed.
- **2026-09-17** — User re-sent the final-sync request. Checked: `main` @ `bfd6d85`, tree clean, the sync is already in place; nothing new to record. `PITCH-DECK.md:87` "0 false positives / 69" still open (T-046a).
- **2026-09-17** — User: fix `PITCH-DECK.md` line 87. Replaced "specificity is proven (0 false positives / 69)" with "0 false positives on 67 judgeable legitimate lab captures (2 more were correctly UNKNOWN); 95% upper bound ≈ 4.4%". Flagged, not changed: the same sentence still calls reproducing the actual exploit future work, but EXP-09 already has a live exploit-lab reproduction.
- **2026-09-17** — User: apply the fixes wherever I had flagged them. `PITCH-DECK.md`: "future work" sentence replaced (EXP-09 live lab reproduction; responder still rejected the Child SA). `.pptx` in Downloads: slide 11 "39 unit tests" → 69; slide 13 "39/39 unit tests … 73 pcaps" → 69/69 … 78 (only these 2 runs changed; rendered via LibreOffice, layout intact). Original kept as `~/Downloads/TunnelScope_SIH26160_Double_Diamond_Deck.before-2026-09-17.pptx`. Neither had the CVE line. Not changed (not flagged earlier): `tunnelscope/README.md:23`, `build/00-ARCHITECTURE.md:109` still say "0 FP/69" (T-058).
- **2026-09-17** — User: no more PPT edits (my 2 slide edits stay); do the repo wording fixes instead. Did T-056 + T-058 in one pass, human-facing text only; kept the `DOWNGRADED` label and rule IDs because 3 renderers and 3 tests match them. Verified: 69/69 unit, 69/69 E2E, dashboard type-check clean, CVE demo output still FAIL (high) with the new note. Not changed (not in scope): PITCH-DECK §7 still says 'two independent implementations' (EXP-10 added OpenBSD iked).
- **2026-09-17** — User asked how TunnelScope is developed and how users use it. Answered from `build/03-USAGE-AND-OPERATIONS-PLAN.md`, `tunnelscope/cli.py` and this session's review loop; no files changed. Noted honestly: there is no live-capture mode (SOC use = rolling capture files), and the local API is still planned, not built.
- **2026-09-17** — User: wants to use it 'more over the dashboard', then clarified they mean literally dropping files into a browser page, not a CLI command per file, and not a rolling/real-time feed (already corrected that expectation in the prior turn). Asked with AskUserQuestion since this ranged from a small doc tweak to a new server; user picked the full build. Delivered T-059: `tunnelscope serve`, a local upload dashboard, stdlib-only, 127.0.0.1-only, uploads validated+deleted. Verified for real: ran the actual server process, drove a real Chromium tab against it, uploaded genuine pcap bytes and confirmed the rendered findings matched the CLI dashboard's known output for that capture, then confirmed a bad upload fails cleanly without disturbing prior results. Not built (flagged, not requested): the SIEM-facing API in the usage plan §6 stays a separate open item.
- **2026-09-18** — User: integrate the upload server with the main dashboard using taste, impeccable, awesome design md and img2threejs. Only taste was installed; asked, user approved fetching impeccable + awesome-design-md (markdown only, no binaries run); img2threejs not found, replaced by installed threejs skills. Delivered T-060 (see Done). Known and stated: three.js is one ~550 KB lazy chunk (named imports don't tree-shake it), main bundle ~675 KB mostly Recharts; one oxlint warning on the queue runner's effect. Not pushed.
- **2026-09-18** — User relayed mentor's advice (application focus: DLP between orgs, stop paper leaks, education/defence users, secure defence data sharing, cloud/private cloud) and asked for a detailed, brutally honest report. Delivered T-061 (`research/13-MENTOR-APPLICATION-REVIEW.md`), web-verified (NEET 2024 CBI findings, remote-access exam gangs, Public Exams Act 2024, AWS S2S VPN allowed algorithms, MeitY GI Cloud, DPDP Rule 6 via secondary source). Verdicts: M1 right, M2 wrong-as-stated, M3 wrong, M4 partial, M5 right with correction, M6 right. Proposed follow-ups A–D (applications slide, on-prem deployment doc, cloud-proposal lab experiment, DPDP mapping) NOT staged as tasks until user decides. No code changed.
- **2026-09-18** — User asked (1) whether, since VPNs aren't named in exam-security law, we can recommend government adopt them for exam security, and (2) what's outstanding. Answered: only narrowly (protects digital transit, not the endpoint leak paths seen in real cases; file-level encryption with timed key release is the more fitting control; TunnelScope is an auditor, not a VPN). Outstanding list given from this file. Re-checked: `pytest` 80 passed (deck says 69 → added as T-046 (d)); `main` is 10 commits ahead of `origin/main` (not pushed); T-061 report not committed.
- **2026-09-18** — User: no separate applications slide; implement every TODO task; mentor-point implementations to be discussed later; full commit/push access. Did T-054, T-055, T-057 (see Done). T-046 left as the user's own steps (upload, video, submit); flagged new counts. Proposed mentor follow-up A dropped (no slide); B–D not started (to discuss). Verified: 97/97 unit, 69/69 E2E, dataset PASS 82 pcaps, differential vs origin/main 0 not-allowed.
- **2026-09-18** — Push was rejected: GitHub `main` had 8 commits (2026-09-14…16) never fetched here, reusing T-051…T-056. Pushed local work to `local-sep17-18`, asked the user which dashboard to keep; user chose black/violet. Did T-068 (merge; see Done). Pushed merged `main`.
- **2026-09-18** — CI on the merge `a3f6f54`: both jobs green; the differential step ran on GitHub's 82 tracked captures (41 changes, all allowed, 0 not allowed). Emptied `build/findings-allow.txt` (entries now merged), stopped the encap lab containers. Branch `local-sep17-18` kept on GitHub as the pre-merge backup.
- **2026-09-18** — User: discuss the mentor follow-ups. Discussion only, no files changed besides this line. Found that B is half-written already (`build/03-USAGE-AND-OPERATIONS-PLAN.md` §2 states offline/air-gapped) but never *tested*; presented B/C/D with effort, value, risks and a recommended order; awaiting user's decisions.
- **2026-09-18** — User: LottieFiles-MCP animation of the tunnel lighting up far-to-near like a horror-film corridor, before the site loads. No LottieFiles MCP exists here (registry searched); asked 4 questions, user chose three.js / full-screen intro / flicker-then-hold / first visit only. Delivered T-069 (see Done). Added `?intro` / `?intro=hold` for demo recording and stills.
- **2026-09-18** — User: make one ring fail and catch twice. Did T-070 (see Done); the rings behind it wait for it.
- **2026-09-18** — User: the intro should play on every reload. Did T-071 (see Done).
- **2026-09-18** — User (with 2 screenshots, canvas selected): make the inside tunnel beautiful and interactive. Did T-072 (see Done), including the off-centre bug visible in their screenshot.
- **2026-09-18** — User (screenshot still showing the pre-T-072 hairline tunnel, off-centre): asked whether the tunnel as a full-screen background would look good, before building it. Answered honestly (only in a restrained, top-of-page form; not behind the data); no code changed; awaiting their choice.
- **2026-09-18** — User: go with all four recommendations for the background tunnel. Did T-073 (see Done).
- **2026-09-18** — User: make the background tunnel a bit brighter. Did T-074 (see Done).
- **2026-09-18** — User: proceed with the mentor follow-ups. Taking my recommended order B → C1 → D (user had not answered the C1/C2 or before/after-submission questions; C2 needs their AWS account, so C1). B done as T-075 (see Done); C1 and D next.
- **2026-09-19** — Resumed after the usage-limit break. Finished C1 as T-076 (see Done). D (Indian-law mapping) next.
- **2026-09-19** — Did D as T-077 (see Done). All mentor follow-ups done except C2.
- **2026-09-19** — User: update deck numbers to 135/87 and use liquid-glass cards with no blur. Asked where (dashboard/deck/both): dashboard only. Did T-078 (see Done).
- **2026-09-19** — User: remove the 'Engine on 127.0.0.1' badge. Did T-079 (see Done).
- **2026-09-19** — User: the intro-to-site transition isn't good; ideate alternatives. Diagnosed the current hand-over and proposed 5 concepts (no code); awaiting the user's pick.
- **2026-09-19** — User picked concept C ('into the core'). Built and verified as T-080 (see Done).
- **2026-09-19** — User asked what was implemented from the mentor conversation. Answered from this file (no code changed): B=T-075, C1=T-076/EXP-13, D=T-077 done; A dropped by the user; DLP and paper-leak claims deliberately not built; C2 open.
- **2026-09-19** — User asked whether the mentor items are really implemented and built. Verified live: T-075/T-076/T-077 commits on origin/main, all files present, 135/135 tests, C1 and D exercised via the real CLI, and the air-gap test re-run on current code: PASS (9 verdicts now, incl. the new RFC8247-DH-OFFER rule; 0 non-loopback connects). Refreshed build/offline/AIRGAP-TEST.md.

- **2026-09-19** — User asked for a `./start.sh` that runs everything needed plus logs. Built and verified as T-081.
- **2026-09-20** — User asked to build anomaly detection, LLM explanations and the Random Forest attacker view, and which models to use. Built as T-082; model choices in `build/07-AI-LAYER.md`.- **2026-09-20** — User asked whether Gemini's API could be used (yes, as a third provider behind the same fact check; paid tier advised because free-tier content may be used by Google; not built) and for a full end-to-end test. Code: 153/153 unit, 69/69 E2E ground truth, dataset PASS (87), every CLI command, API incl. bad inputs and path traversal (403). Browser: intro, sample fleet, 3 uploads + 1 refused, changed chip, AI insights, Ollama rewrite accepted and a fabricated 'MODP-768' rejected (stand-in Ollama server), attacker 98/100, 375px no overflow, engine-offline banner, 0 console errors. Found: the Claude no-credentials message is a raw TypeError (not fixed yet).

- **2026-09-20** — User decided: no outside AI models; every model must be trained by us. Removed the LLM rewrite completely (Claude/Ollama providers, `--llm`, `/api/explain`, dashboard button, `anthropic` extra, start.sh flag). Remaining models: Random Forest attacker and Isolation Forest anomaly detection, both trained on our own data; explanations are template-only. Added a test that fails if an LLM client or network call enters the package. 152/152 tests, dashboard builds.- **2026-09-20** — User asked for a brutally honest validation against the PS. Written to `research/14-PS-VALIDATION.md`: gaps are traffic-type prediction (suppressed), mode (always NOT_OBSERVABLE), replay protection (no finding), threat matrix (none), live capture (none), AH (none), single risk score (refused), AI confidence score (not labelled), demo video. New finding: transport mode is provable from packets below the tunnel-mode size floor (tunnel min 56 B vs transport 36 B in our GCM captures). Nothing changed in code.

- **2026-09-20** — User asked to plan and build every PS gap with failure points and end-to-end tests. Built and verified as T-083. Remaining: demo video (user records it from `build/sih/DEMO-SCRIPT.md`).
- **2026-09-20** — User asked for real-app captures, a second implementation, more repetitions and a mixed-traffic detector. All four done as T-084/EXP-16; the headline is that synthetic traffic does not transfer to real applications (0.46), while the IPsec stack makes no difference (1.00).
- **2026-09-20** — User asked to look for public datasets and open-source alternatives. Survey done (no public IPsec dataset exists; VPN datasets are OpenVPN; several SIH competitor repos claim AI with no validation). Validated against third-party Wireshark IPsec captures instead: found and fixed the ICMP-quoted-header bug (T-085).
- **2026-09-20** — User moving to Antigravity with weaker models; asked Claude to prepare prompts and review. Built the handoff (T-086). Claude reviews via `handoff/review_pack.sh` output.
- **2026-09-21** — README refreshed (T-087, the Antigravity canary task); reviewed by Claude, four false "no result yet" lines and some drifted wording fixed. Evidence: check table.- **2026-09-21** — User is presenting to the judges and asked for the problem statement, the proposed (refined) problem statement and the whole pitch in points. Answered in chat from this file's evidence (no code changed); numbers used: 175 tests, 103 hash-verified captures, 85/85 ground truth, 0.986 held-out, 0.461 synthetic-only on real apps, 1.000 Libreswan, mixed detector 92.9% / 8.3%.

- **2026-09-21** — User asked for an updated deck in the website's fonts and colours, then limited it to 10 slides. Built as T-092 (see Done). Before presenting: change the Video status on slide 10 once the demo video exists, and install the fonts on the presenting machine.
- **2026-09-21** — User asked for a packet dump to demo, the prototype walk-through, and an explanation of every attribute and the stack. Built as T-093; the live-demo instructions were tested exactly as written and a real bug in them fixed.
- **2026-09-21** — Added "How these numbers were measured" panel on Traffic & exposure tab (T-088); evidence: check table (check_all --fast PASS, browser 0 errors, 375px responsive).
- **2026-09-21** — Traffic dataset datasheet generator built (T-089); evidence: check table (check_all --fast PASS, 8/8 datasheet tests pass, 300 sessions / 2,464,991 packets verified).
- **2026-09-21** — EXP-17 network conditions (delay, jitter, packet loss) captured and analysed (T-091); evidence: 96 sessions, 10 IKE bring-ups, check_all --fast PASS, results in experiments/exp17-network-conditions/results/exp17_results.json.
- **2026-09-22** — Added non-blocking external third-party capture validation job to CI (T-090); evidence: check_all --fast PASS, YAML validation verified.
- **2026-09-22** — Retrained traffic classifier and mixed-traffic detector on expanded EXP-17 impaired data (T-092): 160 new sessions captured (reps 4–8, 256 total); leave-one-repetition-out macro-F1 improved from 0.526 clean / 0.953 (3 reps) to 0.9843 (WAN) and from 0.380 clean / 0.840 (3 reps) to 0.9137 (lossy), crossing the 0.90 bar on both profiles; mixed-traffic false-flag rate 0.0% (WAN) and 14.84% (lossy), below the 15% bar; full check_all PASS.
- **2026-09-22** — Judge feedback after the review round: the tool is still "traditional" — it reports, the human still has to patch by hand. Designed (not built) a human-gated remediation loop: propose a per-rule config fix → human Approve/Reject → on approval, apply it, re-capture, re-verify the verdict actually flipped to PASS. Written as `build/09-REMEDIATION-ROADMAP.md` (draft fix commands for 12 of 14 rules; 2 stay advisory-only by design) and a new Q&A entry in `build/sih/PRESENTER-GUIDE.md`; user chose design-only for now, not a build.