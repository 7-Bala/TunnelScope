"""Exploratory mode model (EXP-14 addendum A). Filled in after EXP-15 data is
analysed; until a model passes its pre-declared bar it abstains (returns None)."""
from __future__ import annotations


def infer_mode(record, families):
    return None
