# Decision Log

| ID | Decision | Date | Rationale | Evidence | Reversible? |
|---|---|---|---|---|---|
| DEC-000 | Treat report.md's "IKEProof" concept as a prior hypothesis, not a selection; regenerate concepts from scratch in DEVELOP | 2026-09-09 | Entering DEVELOP with a pre-chosen concept defeats the purpose of the process and produces an undefendable "we just liked it" answer | 00-RESEARCH-PLAN §0.1 | Yes |
| DEC-001 | Methodology = Double Diamond (outer frame) + measurement-study methodology (Discover) + hypothesis-driven experimentation with DoE (Develop) + threat modeling (security engine) + HCD (analyst surfaces) | 2026-09-09 | The binding constraint is measurement feasibility, not user desirability; stock Double Diamond has no feasibility gate | 00-RESEARCH-PLAN §0.2–0.5 | Yes |
| DEC-002 | Introduce a mandatory **Observability Gate**: no capability enters requirements until classified O / M / I / N | 2026-09-09 | Prevents building features on information that does not exist on the wire | 01-DISCOVER §3.5 | No — this is the core discipline |
| DEC-003 | Introduce an explicit **vantage-point taxonomy (V0–V5)** carried through every finding, report and score | 2026-09-09 | "What can be seen" is meaningless without "from where"; the PS omits this entirely | 01-DISCOVER §2 | Yes |
| DEC-004 | Replace the PS's implied full cross-product test matrix with a covering-array / fractional-factorial design | 2026-09-09 | ~8 config axes; full cross-product is combinatorially wasteful and mostly redundant | 00-RESEARCH-PLAN Q14 | Yes |
| DEC-005 | **Analysis posture = passive-first, evidence-tiered, with endpoint-telemetry augmentation (T2) as the PRIMARY escalation and authorization-gated active probing (T4) as a bounded SECONDARY escalation.** Five-tier capability ladder T0-T4; every finding declares its tier; full function at T0/T1; T4 refuses without a logged scope assertion; cross-tier consistency checking is a first-class feature | 2026-09-09 | F-01/F-02/F-05 make key attributes passively unreadable; DISA V-207212 (anti-replay) is a named government control that CANNOT be assessed passively; multi-vendor practitioner guidance already tells engineers to abandon the capture and read configs; USENIX Sec'22 Best Paper uses a staged passive-filter-then-active-prober design; the PS itself mandates building the testbed, so endpoint control is in scope by construction | 06-DISCOVER Part 1 | Yes, but the tier model itself should not be |
| DEC-006 | **Reject pretrained traffic transformers (ET-BERT / YaTC / NetMamba class) as a core engine.** Permitted only as a documented baseline comparison | 2026-09-09 | ET-BERT collapses 98% -> 10.9% under per-flow splits with frozen encoders; randomised weights match pretrained; RF on hand-engineered header features beats all of them; pretraining/downstream dataset reuse inflates results. Adopting them imports the field's credibility crisis into our submission | 04-DISCOVER E-01..E-05 | Yes, on new evidence |
| DEC-007 | **Every compliance verdict must name the authority it is judged against** (RFC 8221/8247/9395 · NIST SP 800-77r1 · DISA VPN SRG · CNSA). No unqualified "compliant" | 2026-09-09 | Contradiction C-2: RFC 8247 baseline is DH group 14; DISA V-207193 requires group 16+. Different risk tiers, not an error | 03-DISCOVER S-02 | No |
| DEC-008 | **UNKNOWN and NOT-OBSERVABLE are first-class verdicts.** Absence of evidence must never render as compliance | 2026-09-09 | False assurance is the most damaging failure mode of an assessment tool (D3 FP/FN table); several controls are unassessable at low tiers | 03-DISCOVER DL-05 | No |
| DEC-009 | **Dataset splitting by capture session at minimum, by configuration cell for config-inference tasks, by implementation and network condition for generalization claims.** Per-packet and within-session per-flow splits are forbidden | 2026-09-09 | Per-packet splitting leaks flow identity via TCP seq/timestamps and is the identified mechanism behind inflated published results | 05-DISCOVER sec 6; arXiv:2507.16438 | No |
| DEC-010 | Adopt **`Contradictory`** as a first-class measurement outcome alongside UNKNOWN / NOT-OBSERVABLE, with contradictions preserved in output rather than reconciled | 2026-09-09 | Taken from the PQ-TLS multi-surface framework (arXiv:2605.02978). In IPsec this is not an edge case but a product feature: cross-tier disagreement (T0/T1 inference vs T2/T3 truth) is both our validation signal and a real security finding | 08-DISCOVER sec 4.2 | No |
| DEC-011 | Hold inference rules, algorithm registries and baseline mappings as **versioned data outside parser code** | 2026-09-09 | Same source. Makes multi-baseline assessment (DEC-007) tractable and the engine auditable; RFC 9395's IANA "Status" column, RFC 8221/8247 requirement levels and DISA rule IDs all become data | 08-DISCOVER sec 4.2 | Yes |
| DEC-012 | **Do not claim novelty for the observability framework itself.** Claim the IPsec/IKEv2 instantiation and the data-plane extension (G-13), citing arXiv:2605.02978 as convergent prior art for TLS | 2026-09-09 | The framework exists for TLS, is benchmarked and released, and is explicitly TLS-only. Convergent independent derivation is more persuasive to a jury than an unsupported originality claim - and an unchecked claim would be falsifiable in one search | 08-DISCOVER sec 2-4 | No |
| DEC-013 | Correct F-04's claimed strength: the ESP sieve is a one-directional CBC-vs-AEAD/stream filter, not a general cipher-family classifier. Asymptote is 5-6 candidates for the AEAD/CTR/stream cluster, not <=2 | 2026-09-10 | EXP-01 met its own pre-registered falsification criterion; mathematical proof of containment found (align_A multiple of align_B + iv delta multiple of align_B => one-directional non-elimination) | experiments/exp01-cipher-sieve/, experiments/RESULTS.md | No — this is now a proven mathematical property, not an estimate |
| DEC-014 | Correct 07-DISCOVER: strongSwan 6.0.2's ML-KEM support is via the native `ml` plugin, NOT liboqs/oqs or Botan. No third-party PQ crypto library required | 2026-09-10 | Verified against actual strongSwan 6.0.2 `./configure --help` and source tree; a full liboqs build was written and compiled successfully before this was discovered, then removed as unnecessary | testbed/NOTES.md #9, testbed/images/strongswan-pq/Dockerfile | No |
| DEC-015 | Promote CS-05 (PQ-readiness/downgrade observability) to highest priority for DEVELOP concept generation | 2026-09-10 | EXP-04 confirmed 4 independent deterministic signals, strongest requiring zero length arithmetic; fully deterministic (AI Necessity Matrix entry confirmed, not just claimed); ties to DST National Quantum Mission policy found in Discover | experiments/exp04-pq-length-asymmetry/, experiments/RESULTS.md | Yes, pending DEVELOP's weighted matrix |
| DEC-016 | Caveat on DEC-005's T3 (keying material): on kernels in lockdown-confidentiality mode, SA keys cannot be read back from XFRM. T3 is an audit/lab tier, not a production assumption | 2026-09-11 | strongSwan 6.1.0 release notes: "we don't get the keys back when we query an SA" under kernel lockdown | 11-EXISTING-SOLUTIONS-DEEP-DIVE.md §3.C, RL-033 | Yes |
| DEC-017 | Withdraw "Wireshark cannot decode PQ IKE". Restate CS-05 as PQ *posture assessment + downgrade detection*, built on and cross-checked against Wireshark's ADDKE parsing; keep EXP-04's structural signals for SOC tools that can't parse ADDKE (Suricata, Zeek, nDPI, Arkime) | 2026-09-11 | Wireshark #21072 closed 2026-03-14; master names ML-KEM; SOC-side parsers verified not to handle ADDKE | 11-EXISTING-SOLUTIONS-DEEP-DIVE.md §6, RL-029/030 | No — the correction is factual |
| DEC-018 | Failure-mode diagnosis (CS-02) is deterministic: rules with cited protocol arithmetic, not a trained classifier. Proposal-mismatch and TS-mismatch are reported as one class ("child SA rejected — reason needs T2") | 2026-09-12 | EXP-06 r2: rules = fitted tree at macro-F1 1.000 (6-class); zero-variance features; F2/F3 provably inseparable at T0/T1 | experiments/exp06-failure-diagnosis/RESULT_R2.md | Yes, if a future implementation shows noisy signatures |
| DEC-019 | PFS detection is deterministic (one CREATE_CHILD_SA length threshold), not "ML, narrow" | 2026-09-12 | EXP-03: fixed 256-byte gap; EXP-06 r2 reproduced the same 512-byte PFS request | experiments/exp03-pfs-signature/, 09-DEFINE.md | Yes |
| DEC-020 | The PQ detector's decisive signals are IKE_INTERMEDIATE presence and the ADDKE transform (8 B each, plaintext). The INTERMEDIATE_EXCHANGE_SUPPORTED notify and the fragmentation pattern are corroborating evidence and implementation fingerprints, never sufficient alone | 2026-09-12 | EXP-07: the notify tracks ADDKE on strongSwan but tracks the `intermediate=yes` capability on Libreswan — a notify-only detector would false-positive | experiments/exp07-libreswan-generalization/RESULT.md | No |
| DEC-021 | Metadata-exposure output is per-channel leakage (bits/packet for size/timing/direction), adversary capability and a Bayes-error bound — NEVER an asserted traffic-type label for a tunnel | 2026-09-12 | EXP-05: mixtures produce confident wrong labels (video+interactive → "web" 100%); the leakage metric is stable and passes its null | experiments/exp05-metadata-leakage/RESULT.md | No |
| DEC-022 | Padding recommendations must state that TFC padding hides sizes but NOT the traffic class from a timing-aware observer, and what it costs (+54% here) | 2026-09-12 | EXP-05: padding zeroed size MI and left F1 at 0.995 | same | Yes, if constant-rate IP-TFS becomes testable |
| DEC-023 | **DEVELOP selection (GATE-5): K1 — evidence-tiered posture engine (C3) + PQ/downgrade assessor (C6) + metadata-leakage module (C8); endpoint cross-check (C5) optional.** Build in stages: C3+C6 MVP → C8 → C5. Rejected: C1, C7, K2; C4 deferred | 2026-09-12 | Weights committed before scoring (6425af2); K1 wins 22/23 weighting scenarios; the one flip (feasibility ×3 → C6 alone) sets the build order and the fallback | research/12-DEVELOP.md, data/develop_*.json | Yes, on the triggers in doc 12 §8 |
| DEC-024 | Tunnel/transport mode is NOT-OBSERVABLE at T0 (EXP-08). Reported from T2, or a gateway-vs-host topology heuristic with stated confidence, else NOT-OBSERVABLE. No ML component. This leaves metadata-leakage measurement (CS-01) as the project's ONLY justified ML component | 2026-09-12 | EXP-08: fixed +20 B offset only with a paired baseline; without one, every ESP length is valid in both modes (same GCM residue class) | experiments/exp08-mode-inference/RESULT.md | No |
| DEC-025 | **PQ selection is read from the responder's plaintext IKE_SA_INIT proposal (OBSERVED), not inferred from IKE_INTERMEDIATE absence.** An error-only response is UNKNOWN. Cause of a classical selection (policy, second proposal, induced retry) is not attributed at T1. Value string `offered-but-not-used` kept as the consumer contract. Refines DEC-020 | 2026-09-17 | T-052: `pq-downgrade.pcap`'s responder SA carries no additional key exchange; the old inference also raised a false 'possible downgrade' on a legitimate two-proposal offer | build/04-IMPLEMENTATION-PLAN.md §3.1 | No |
| DEC-026 | **ESP-only captures: one record per host pair across SPI changes** (a new SPI pair = rekey of the same tunnel); several IKE SAs between one pair split ESP by time window. PFS size rule applies to MODP groups only; other groups UNKNOWN until calibrated | 2026-09-17 | T-052: splitting per SPI turned one rekeying tunnel into 4 one-way records and collapsed timing leakage 1.97 → 0.0–0.58 bits; no capture calibrates non-MODP PFS sizes. User decision on the ESP-only rule | build/04-IMPLEMENTATION-PLAN.md §3.2, §4, §6 | Yes, if parallel tunnels between one pair must be separated |
| DEC-027 | **Supersedes DEC-021 (PS c "predict type of traffic"): the traffic type inside ESP IS shown**, as an INFERRED finding with its calibrated probability and the next alternatives, only when it clears an abstain rule (confidence and window agreement, in distribution); otherwise UNKNOWN "uncertain" with the reason. Known confusions (e.g. video+interactive reads as web) are stated with the output | 2026-09-20 | The PS asks for it; DEC-021's failure (confident wrong labels on mixtures) is now a measured abstain case, not a reason to hide every answer | experiments/exp15-traffic-classes-suites-ah/RESULT.md, research/14-PS-VALIDATION.md | Yes |
| DEC-028 | **Amends DEC-007's "no single number": one overall RISK score** (0-100, noisy-OR over threats present in a threat matrix) is shown, always with its drivers, the threats that could not be assessed (coverage) and the per-baseline compliance scores underneath. It is a risk score, never a compliance score; 0 means "no threat seen", stated as not-safe | 2026-09-20 | PS e) asks for a risk score and a comprehensive security score; showing the breakdown keeps DEC-007's point (which authority, which rule) visible | tunnelscope/risk/risk.py, tests/test_ps_gaps.py | Yes |
| DEC-029 | **Amends DEC-024 (mode):** mode is OBSERVED from AH's plaintext next header; for ESP, transport is INFERRED when a packet is below the tunnel-mode size floor (EXP-14); tunnel mode is never claimed from ESP traffic alone; otherwise UNKNOWN | 2026-09-20 | EXP-08 held for large packets but not small ones; AH was never analysed before | experiments/exp14-mode-size-floor/ | Yes |
| DEC-030 | **Live streams by windows:** live analysis = closed capture windows (dumpcap ring buffer, or a sensor's rotating files), each run through the normal pipeline and the anomaly history. No packet-by-packet streaming engine | 2026-09-20 | Reuses every tested invariant unchanged; bounded memory/disk; works with sensors that already rotate files | tunnelscope/live/live.py | Yes |
| DEC-031 | **Narrows the "every model is trained by us" rule, does not drop it:** an optional, off-by-default, fully on-device MLX language model may REPHRASE already-computed explanation/remediation text; it can never originate a finding, verdict, score, or remediation command, and touches no network. Every model that decides something stays the project's own, trained on our own data, unchanged | 2026-09-22 | User asked for local AI models via MLX (good on Apple Silicon); this is the third time text generation has been asked for (cloud LLM declined 2026-09-21, an LLM rewrite built and removed 2026-09-20) — MLX resolves the network/data-leaves-the-machine objection but not the "not trained by us" one, so the rule is split into a decides/rephrases boundary instead of relaxed wholesale | build/10-LOCAL-AI-RUNTIME.md | Yes — additive, off by default, not built yet |
| DEC-032 | **Reject an AI model (LLM or otherwise) as the zero-day/unknown-vulnerability detector; the existing Isolation Forest anomaly layer already does that job.** Permit a new, stricter "suggest" tier for the same local MLX model: once the anomaly layer flags a deviation, it may draft candidate things a human should check, never a diagnosis, with every rule ID/CVE/cipher/verdict token stripped from its output before display | 2026-09-22 | User asked whether AI can bridge the zero-day gap. Same reasoning as DEC-006 (pretrained transformer traffic models rejected as core detector: 98%→10.9% under honest splits, beaten by a plain RF) applies to an LLM detector — no model recognizes what it never saw, and this one isn't even given raw packets. What genuinely helps is triage, not detection, and it needs a stricter guardrail than DEC-031's rephrase tier since it drafts new text rather than reording given text | build/10-LOCAL-AI-RUNTIME.md, DEC-006 | Yes — additive, off by default, not built yet |
