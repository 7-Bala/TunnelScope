# Air-gapped install test (mentor follow-up B)

Generated by `build/offline/airgap_test.sh` on 2026-09-18T16:24Z. Result: **PASS**.

Bundle: `tunnelscope-offline-0.3.0-linux-aarch64-py311.tar.gz` ( 57M), built in a network-on staging container.
Target: same image, `docker run --network none`, everything under `strace -f -e trace=connect`.

```
CHECK network: no default route, only loopback
CHECK checksums: every wheel matches SHA256SUMS
CHECK install: pip --no-index from the bundle only
tunnelscope       0.3.0
tshark            4.0.17  (/usr/bin/tshark)
required fields   all 29 resolve on this tshark
baselines         4 loaded: CVE-WATCH, DISA-VPN-SRG-V2R6, DST-NQM-2026, RFC-8247

ready: evidence extraction will not silently under-report on this stack.
CHECK assess: 8 verdicts across ['CVE-WATCH', 'DISA-VPN-SRG-V2R6', 'DST-NQM-2026', 'RFC-8247']; DST-PQ-DOWNGRADE = FAIL
CHECK serve: /health ok, packaged dashboard found
CHECK serve: dashboard page served from the installed package
CHECK upload: /api/analyze ok on a real IPv6 capture, 1 record(s)
---- connect() attempts by every process (strace -f):
      7 sa_family=AF_INET, sin_port=htons(8765), sin_addr=inet_addr("127.0.0.1")
     70 sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"
CHECK outbound: zero connection attempts beyond loopback / local sockets
```
